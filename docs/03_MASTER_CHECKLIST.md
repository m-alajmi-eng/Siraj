# 03 — القائمة الرئيسية الشاملة (Master Checklist)

> **أهم وثيقة تنفيذية في المشروع.** كل بند له: حالة، أولوية (P0 حرجة قبل أي
> إطلاق / P1 قبل الإنتاج / P2 تحسين مجدول / P3 مستقبلي)، ولماذا يهم، وخطوات،
> وخطر التجاهل. الحالات: ✅ مكتمل ومتحقق · 🟡 جزئي · ⬜ لم يبدأ · ❌ محظور
> (يمنعه عائق) · ⚠️غ = غير متحقق، يحتاج مراجعة فعلية للمستودع.
>
> **قاعدة ذهبية:** لا يتحول بند إلى ✅ إلا بتحقق فعلي (تشغيل/فحص/اختبار)،
> لا بكتابة الكود فقط.

---

## أ) المعمارية (Architecture)

| البند | الحالة | أولوية | لماذا/الخطوات/الخطر |
|---|---|---|---|
| Feature-first + فصل data/domain/presentation | ✅ | — | قائم في quran/khatmah/prayer. الخطر عند الكسر: تشابك يصعّب الصيانة |
| اتجاه التبعية (presentation→domain→data) | 🟡 | P2 | سليم عموماً؛ استثناء موثّق: MoreScreen داخل الراوتر. خطوة: نقلها لملفها (دين M1) |
| ADRs محدّثة | 🟡 | P1 | ADR-007 يحتاج تحديثاً بالمصدر النهائي المختار للمصحف بعد حسمه. الخطر: وثيقة مضللة لعلاء |
| معالجة أخطاء موحّدة عبر الطبقات | 🟡 | P2 | حالياً try/catch متفرقة + fallbacks جيدة في البيانات. خطوة: نمط Result/استثناءات موحّد عند أول ألم فعلي |
| التنقّل: مسارات موثّقة ومنطقية | 🟡 | P2 | Shell + مستقلة يعمل. مشكلة UX موثّقة: زر العودة (قسم UX) |
| Offline-first كعقد معماري | ✅ | — | ADR-006 منفّذ ومختبر بقطع الإنترنت |
| SME (محرك مصحف بواجهة معزولة عن المصدر) | ⬜ | P1 | قرار موثّق: طبقة عازلة تسمح بتبديل مصدر العرض دون لمس التطبيق. تُبنى مع إنهاء وضع الصفحات |

## ب) جودة الكود (Code Quality)

| البند | الحالة | أولوية | ملاحظات |
|---|---|---|---|
| flutter analyze صفر أخطاء | ✅ | — | منضبط عبر الجلسات (قاعدة عمل) — لكن يدوياً؛ يجب نقله لـ CI |
| صفر تحذيرات/lints | ⬜ | P2 | متراكم معروف: print()، withOpacity، unnecessary_underscores، unused imports. خطوة: جلسة تنظيف واحدة + تشديد lints |
| إزالة الكود الميت | ⬜ | P2 | متحقق واحد: UthmanHafs.ttf صفري الحجم يُحذف. البقية ⚠️غ تحتاج جرداً |
| لا `dynamic` بلا مبرر | 🟡 | P2 | palette في widgets الختمة dynamic — يعمل لكن يفقد type safety. خطوة: نوع ThemePalette صريح |
| توثيق الكود (dartdoc للعناصر العامة) | 🟡 | P3 | موجود بالعربية في الملفات الجديدة؛ غير منتظم في الأقدم |
| صفر TODO بلا تذكرة | ✅ | P2 | جرد فعلي منفَّذ (`grep -rnE "//\s*TODO\|#\s*TODO\|TODO\(\|FIXME\|XXX:"` عبر كامل المستودع ما عدا build/‏.dart_tool/‏assets الترجمات). **النتيجة: صفر TODO/FIXME في `lib/` أو `test/`** (كود التطبيق نفسه نظيف تماماً). 3 نتائج فقط خارج كود التطبيق، كلها موثّقة كتذاكر هنا: (1) `android/app/build.gradle.kts:19` تعليق قالب `flutter create` عن applicationId — **باطل فعلياً**؛ الـapplicationId مخصَّص أصلاً لـ`app.siraj.siraj`، فالتعليق نفسه متروك خطأً (P3 تنظيف تعليق فقط، لا قرار مطلوب). (2) `android/app/build.gradle.kts:31` عن توقيع الإصدار — **يكرّر تذكرة موجودة فعلاً**: "توقيع release مؤمَّن ⚠️غ P0" في قسم ك (CI/CD)، لا تذكرة جديدة. (3) `linux/flutter/CMakeLists.txt:9` تعليق داخل قالب Flutter SDK نفسه (يُنشئه/يُحدّثه `flutter create`)، ليس كوداً نملكه أو نعدّله — لا تذكرة |

## ج) الأداء (Performance)

| البند | الحالة | أولوية | ملاحظات |
|---|---|---|---|
| قياس زمن الإقلاع (جهاز ضعيف) | ⬜ | P1 | غير مقاس إطلاقاً. الخطر: ANR على أجهزة الجمهور المستهدف. أداة: DevTools + جهاز حقيقي رخيص |
| ذاكرة الأصول (27MB+ JSON دائم) | ⬜ | P1 قياساً، P2 حلاً | H3: يُقاس أولاً؛ الحل (كسول/SQLite) فقط عند ألم مقاس — قاعدة مبدئية |
| تحليل JSON خارج خيط الواجهة (compute/isolate) | ⬜ | P2 | حالياً jsonDecode على الخيط الرئيسي عند أول تحميل. خطوة سهلة عالية الأثر عند القياس |
| rebuilds محسّنة (const، select) | ⚠️غ | P2 | يحتاج DevTools inspector |
| أداء تقليب صفحات المصحف 60fps | ⬜ | P1 | جوهر التجربة الجديدة — يُقاس فور بناء الوضع النهائي |
| ضغط الأصول (تشغيل flutter build بأعلام الانكماش) | ❌ | P0 | محظور بـ C1 — يُختبر فور إصلاح البناء |

## د) الأمان (Security)

| البند | الحالة | أولوية | ملاحظات |
|---|---|---|---|
| Supabase RLS مفعّل على كل الجداول | ✅ | **P0** | **مراجعة فعلية كاملة عبر `supabase db advisors --type security` + استعلام `pg_class.relrowsecurity` على الـ16 جدولاً في public، ثم إصلاح مُطبَّق فعلياً على الإنتاج.** كانت 3 جداول (`surahs`, `tafsir_sources`, `hadith_books`) بلا RLS وanon يملك عليها DELETE/INSERT/UPDATE/TRUNCATE فعلياً — ثغرة P0 نشطة، سبقت event trigger `ensure_rls` الذي يُفعّل RLS تلقائياً على الجداول الجديدة فقط. تم `supabase db push` لـ`20260714030138_enable_rls_missing_tables.sql` (نفس نمط "Public read access" المستخدم في بقية الـ13 جدولاً)، وأُعيد فحص `supabase db advisors` بعدها فتأكد اختفاء الثلاث تحذيرات وأن `relrowsecurity=true` + سياسة واحدة لكل جدول. كل الـ16 جدولاً في public الآن سليمة. فحصنا أيضاً دالتي `delete_user` و`rls_auto_enable` القابلتين للتنفيذ من anon (تحذير WARN متبقٍّ) — كلتاهما آمنتان فعلياً: الأولى تتقيّد بـ`auth.uid()` (لا تأثير لغير المسجّلين)، والثانية event trigger لا يمكن استدعاؤها كـRPC عادي |
| الأسرار خارج الكود / gitignore سليم | ⚠️غ | P0 | مراجعة فعلية للمستودع + تاريخ git (لو تسرّب مفتاح: تدويره) |
| flutter_secure_storage لرموز Auth | ⬜ | P1 | مخطط موثّق غير منفّذ |
| تحقق من المدخلات (نماذج الإنشاء) | 🟡 | P2 | الختمة تتحقق أساسياً؛ جرد بقية النماذج ⚠️غ |
| فحص ثغرات التبعيات | 🟡 | P2 | `dart pub outdated` نُفِّذ فعلياً (2026-07-15، على `main`) — **تقرير فقط، صفر تحديث تبعيات تلقائي** بقرار: بعد حادثتي ترقية `sentry_flutter` و`flutter_local_notifications` السابقتين، أي ترقية تبعية تحتاج مراجعة علاء المباشرة تحديداً، لا قراراً آلياً. **تبعيات مباشرة متأخرة**: `firebase_auth` 6.5.4→6.5.6، `firebase_core` 4.11.0→4.12.1 (ترقيات patch بسيطة) · `intl` 0.20.2→0.20.3، `timezone` 0.10.1→0.11.1 (Resolvable فقط، ليست Upgradable ضمن القيد الحالي) · **`flutter_local_notifications` 19.5.0 على `main` الآن (قبل دمج #14/#4)، وأحدث إصدار متاح 22.0.1 — أي أحدث حتى من الـ20.1.0 التي تُعالَج حالياً في PR منفصل. يحتاج قرار علاء المباشر: هل نكتفي بـ20.1.0 الحالية أم نقفز مباشرة لـ22.0.1؟ (احتمال كسر API إضافي شبيه بما عولج في fix/flutter-local-notifications-named-params)**. تبعيات عابرة (transitive) متعددة متأخرة أيضاً (`analyzer`، `_fe_analyzer_shared`، `firebase_core_platform_interface` وغيرها) لكنها تُدار تلقائياً عبر SDK/الحزم المباشرة، لا تحتاج تدخلاً مباشراً. الأتمتة في CI لم تُفعَّل بعد (تبقى ⬜ فرعياً) |
| تعتيم الكود عند release | ⬜ | P1 | مخطط موثّق (--obfuscate) — بعد إصلاح البناء |

## هـ) الاختبارات (Testing)

| البند | الحالة | أولوية | ملاحظات |
|---|---|---|---|
| Unit: KhatmahPlan (كل الحسابات المشتقّة) | ✅ | **P0** | `test/khatmah_plan_test.dart` يغطي dailyPortion/pagesAheadOrBehind/todayPortionRange بحالات حدّية (اليوم الأول بلا قراءة، منتصف الخطة على/فوق/تحت المخطط، اليوم الأخير، تجاوز المدة، سجل فارغ، اكتمال ورد اليوم، قرب نهاية المصحف). `flutter test` ← 13/13 نجاح |
| Unit: ثوابت البيانات الدينية | ✅ | **P0** | `test/mushaf_data_test.dart` يفتح `assets/data/quran_uthmani.json` و`quran_translations.json` و`surah_names.json` فعلياً ويؤكد: 6236 آية، 604 صفحة (1→604 بلا فجوات)، 114 سورة، 14 لغة ترجمة، صفر نصوص فارغة. `flutter test` ← 7/7 نجاح |
| Unit: CacheService + datasources fallback | ✅ | **P1** | `test/cache_service_fallback_test.dart`: (أ) CacheService — round-trip فعلي عبر Hive حقيقي لـsettings/موضع القراءة/سياق القراءة الموحّد (بما فيه مسح khatmahId عند التبديل لسورة)/خطط الختمة. (ب) `QuranRemoteDataSource` — يتحقق أن `getAyahs` يعتمد الأصول المحلية أولاً (مطابقة فعلية لملف quran_uthmani.json الخام) دون شبكة، وأن `getAyahs`/`getTafsir` يسقطان فعلياً لذاكرة Hive المخزَّنة مسبقاً عند غياب سورة/آية من الأصول المحلية (سورة وهمية 999) — بلا أي محاولة اتصال شبكة/Supabase فعلية. `flutter test` ← 7/7 نجاح (27/27 إجمالاً على هذا الفرع) |
| Widget: شاشتا الختمة | ⬜ | P1 | إنشاء خطة → ظهور بطاقة بالحسابات الصحيحة |
| Golden: صفحة مصحف مرجعية | ⬜ | P1 | لقطة ذهبية لصفحة 1 و604 — أي تغيير بصري غير مقصود يفشل الاختبار |
| Integration: تدفق قراءة كامل | ⬜ | P2 | بعد استقرار وضع الصفحات |
| اختبار يدوي موثّق على جهاز ضعيف + RTL/LTR | ⬜ | P1 | قائمة سيناريوهات مكتوبة تُنفَّذ قبل كل إصدار |

## و) واجهة الاستخدام (UI)

| البند | الحالة | أولوية |
|---|---|---|
| نظام التصميم (tokens/typography/ألوان) | ✅ | — |
| RTL/LTR في 15 لغة | ✅ (تحقق بصري متكرر) | — |
| حالات فارغة/تحميل/خطأ في كل شاشة | 🟡 (الجديدة نعم؛ الأقدم ⚠️غ) | P1 |
| توحيد زر العودة (widget موحّد) | ⬜ (موثّق ROADMAP، ملاحظة مستخدم فعلية) | P1 |
| استجابة الشاشات الكبيرة (iPad/سطح مكتب) | ⬜ (مؤجَّل موثّق) | P2 |
| وضع صفحات المصحف: إصلاح اسم السورة المشوّه + أزرار تنقّل | 🟡 | **P0 للقسم** |

## ز) تجربة الاستخدام (UX)

| البند | الحالة | أولوية |
|---|---|---|
| Onboarding يعمل | ✅ (قائم) | — |
| توحيد متابعة القراءة عبر نقاط الدخول + تمرير دقيق للآية | ⬜ (مرحلة 6 ختمة، مؤجَّل بقرار) | P1 |
| إمكانية الوصول: TalkBack/VoiceOver/تباين/خط كبير | ⬜ غير مختبر إطلاقاً | P1 قبل إنتاج |
| اكتشاف الميزات (وضع الصفحات/الختمة ظاهران) | 🟡 | P2 |

## ح) تحقق المحتوى القرآني (Quran Validation) — **قسم لا تهاون فيه**

| البند | الحالة | أولوية | ملاحظات |
|---|---|---|---|
| النص العثماني 6236 آية مصدر موثوق | ✅ (تحقق عند الدمج) | — | يتحول لاختبار آلي دائم (قسم هـ) |
| ترقيم الصفحات 604 مطابق | ✅ (تحقق برمجي عند الدمج) | — | كذلك |
| التفسير الميسّر 6236 مطابق | ✅ (تحقق يدوي لعينات + عدّ كامل) | — | |
| 14 ترجمة كاملة | ✅ عدّاً؛ 🟡 جودةً | P1 | مراجعة عينات بشرية لكل لغة لم تتم — خطوة: مراجع ثقة لكل لغة (علاء وغيره) |
| التجويد الملوّن: صحة الأحكام | 🟡 | **P1** | عُرض وتحقق بصرياً كعينات؛ **مصدر البيانات وترخيصه ودقته الشرعية الشاملة: غير محسوم** — يحتاج توثيق مصدر + مراجعة مختص |
| علامات الوقف تظهر صحيحة بخط HafsSmart | 🟡 (ظهرت بصرياً في الاختبار) | P1 | فحص منهجي لعينة صفحات |
| نص الشهادة `needs_verification` | ✅ | — | راجعه المطوّر شخصياً (نص عربي + ترجمة إنجليزية + نقل صوتي) وتأكّد من صحتها، موثّق في البيانات نفسها (verified_by/verified_date، 2026-07-10). النص مطابق للصيغة الثابتة في السنة (صحيح مسلم عبر صحيح الجامع) |
| تطابق وضع الصفحات مع المصحف المطبوع | 🟡 | P1 | الفجوة موثّقة (line-locked) — القرار في QURAN_SECTION_CHECKLIST |

## ط) مراجعة الميزات (Features)

| الميزة | الحالة | ملاحظة |
|---|---|---|
| القرآن (3 أوضاع) | 🟡 | وضع الصفحات قيد الإنهاء |
| الختمة | 🟡 | مراحل 3–7 متبقية (KHATMAH_DESIGN) |
| الصلاة + الأذان | ✅ | مختبرة |
| الأذكار | ✅ قائم | مراجعة محتوى دورية |
| المكتبة | ✅ قائم | ترخيص IslamHouse معلّق (P1) |
| الراديو | 🟡 | يعمل؛ ترخيص البث ❓ (P0 قانوني) + خلفية/foreground service ⬜ |
| القبلة | 🟡 | تعمل؛ حزمة flutter_qiblah نفسها سبب فشل البناء — قرار: تحديث أو استبدال (P0) |
| البحث | 🟡 قائم أساسي | FTS لاحقاً بمعيار |
| قصص + أطفال | 🟡 غير مكتمل | قرار للإصدار الأول: إخفاء أو إكمال (P1) |
| البوابة | ✅ | كاملة 15 لغة |
| الإعدادات | ✅ قائم | يُضاف إليها: حذف الحساب + صفحة التراخيص (P0) |

## ي) قاعدة البيانات (Supabase)

RLS ⚠️غ **P0** · جرد المخطط والفهارس ⚠️غ P2 · سياسات Storage ⚠️غ P1 ·
خطة نسخ احتياطي ⬜ P1 (تصدير دوري — المحتوى الحرج أصلاً محلي في التطبيق
والمستودع، وهذا يخفف الخطر) · migrations منهجية ✅ (تحقق: `supabase
migration list` يؤكد تطابق local/remote بالضبط — `00000000000000_baseline`
و`20260714010124_add_table_comments` مطبقتان على القاعدة الفعلية؛ أي تغيير
مخطط الآن يمر عبر `supabase migration new` + ملف SQL في Git، لا لوحة
التحكم مباشرة).

## ك) CI/CD

GitHub Actions (analyze+test+build APK) ✅ (تحقق: خطوة `flutter build apk
--release` مضافة بعد analyze+test في `.github/workflows/ci.yml`، واختُبرت
محلياً بنجاح: `✓ Built build/app/outputs/flutter-apk/app-release.apk
(167.8MB)`) · توقيع release مؤمَّن ⚠️غ P0 · إصدارات semver + changelog ⬜ P1 ·
بناء iOS سحابي (يتطلب macOS runner) ⬜ P1 · lint gate ⬜ P1.

## ل) المراقبة (Monitoring)

Crashlytics ⬜ **P0** · Analytics بخصوصية (أحداث مجمّعة فقط، توثيق كل حدث
في سياسة الخصوصية) ⬜ P1 · استبدال print بـ logger مستويات ⬜ P2 ·
مراقبة أداء (Firebase Performance) ⬜ P2.

## م) القانوني (Legal)

سياسة خصوصية ⬜ **P0** · شروط استخدام ⬜ P0 · حذف حساب ⬜ **P0** ·
صفحة تراخيص داخل التطبيق (KFGQPC EULA كما هو + SIL OFL + مصادر البيانات
+ الحزم) ⬜ **P0** · ترخيص الراديو ❓ **P0** · شروط IslamHouse ❓ P1 ·
مصدر بيانات التجويد ❓ P1 · سياسة احتفاظ بالبيانات ⬜ P1 ·
حقوق اسم/شعار سراج (فحص علامة تجارية أساسي) ⬜ P2.

---

## خلاصة P0 (ترتيب التنفيذ المقترح)

1. إصلاح البناء (يفتح كل شيء) → 2. GitHub Actions → 3. اختبارا KhatmahPlan
+ الثوابت الدينية → 4. مراجعة RLS والأسرار → 5. إنهاء وضع الصفحات (اسم
السورة + أزرار + قرار line-locked) → 6. حزمة القانوني (خصوصية/حذف/تراخيص/
شهادة/راديو) → 7. Crashlytics.
