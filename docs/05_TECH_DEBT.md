# 05 — سجل الديون التقنية (Technical Debt Register)

> كل دين مصنَّف: الأثر (Impact) · الأولوية · الصعوبة · الجهد التقديري ·
> الخطر إن تُرك · الإصلاح الموصى. **قاعدة:** الدين المقبول بوعي وموثّق ليس
> عيباً — العيب هو الدين المنسي. يُحدَّث هذا السجل عند كل قرار "سنصلحه لاحقاً".

| ID | الدين | الأثر | أولوية | صعوبة | جهد | الخطر إن تُرك | الإصلاح |
|---|---|---|---|---|---|---|---|
| TD-01 | ~~تثبيت/تعارض Gradle 9.1×Kotlin 2.3.20~~ **[محلول يوليو 2026]** | ~~حرج~~ | ✅ | — | — | — | حلّ عبر: إزالة flutter_qiblah (غير مستخدمة)، android.r8.proguardAndroidTxt.disallowed=false، isCoreLibraryDesugaringEnabled. release APK يبني بنجاح (160.4MB) |
| TD-02 | صفر اختبارات | حرج | **P0** | سهلة البداية | يوم للأساس | كسر صامت لمحتوى ديني | KhatmahPlan + ثوابت البيانات أولاً، ثم golden لصفحة مصحف |
| TD-03 | ذاكرة: static caches ~27MB+ JSON دائم | عالٍ | P1 قياس/P2 حل | متوسطة | 2–3 أيام للحل الكامل | بطء/ANR على أجهزة ضعيفة؛ يتضاعف مع كل مصدر جديد | قياس أولاً؛ ثم كسول لكل سورة أو SQLite. **لا يُنفَّذ قبل قياس** |
| TD-04 | jsonDecode على خيط الواجهة عند أول تحميل | متوسط | P2 | سهلة | ساعات | تجمّد لحظي محسوس عند الإقلاع على أجهزة ضعيفة | compute()/isolate للملفات الكبيرة |
| TD-05 | MoreScreen معرّفة داخل app_router.dart | متوسط | P2 | سهلة | ساعة | تضخم ملف الراوتر وخلط مسؤوليات | نقلها لـ features/more/presentation/screens |
| TD-06 | print() في الإنتاج + lints متراكمة (withOpacity، underscores، unused imports) | منخفض–متوسط | P2 | سهلة | نصف يوم | ضجيج يخفي تحذيرات مهمة؛ حرج أداء طفيف | جلسة تنظيف + تشديد analysis_options + lint gate في CI |
| TD-07 | dynamic palette في widgets الختمة | منخفض | P2 | سهلة | ساعة | أخطاء نوع لا يكتشفها المحلل | نوع صريح للـ palette واستخدامه |
| TD-08 | ملف يتيم UthmanHafs.ttf (0 بايت) + خط QuranFont هو فعلياً Scheherazade باسم مضلل | منخفض | P2 | سهلة | ساعة | التباس دائم للمطورين (حدث فعلاً معنا) | حذف اليتيم؛ إعادة تسمية العائلة أو توثيق حقيقتها في pubspec |
| TD-09 | التمرير الدقيق لموضع الآية مؤجَّل | متوسط UX | P1 (مرحلة 6 ختمة) | متوسطة | يوم | "متابعة القراءة" تفتح أول السورة — إحباط يومي | يُنفَّذ ضمن توحيد متابعة القراءة كما هو موثّق |
| TD-10 | ~~أزرار تنقّل صفحات المصحف غير مبنية + سحب سطح المكتب ثقيل~~ **[مؤكَّد محلول مسبقاً — تحقق 2026-07-15]** | ~~متوسط~~ | ✅ | — | — | — | **تحقق فعلي من الكود** (`page_reader_screen.dart`): زرّا `chevron_right`/`chevron_left` + `_goToPage()` موجودان وفعّالان (شريط سفلي)، بالضبط كما اقترح هذا السطر نفسه سابقاً ("_goToPage + شريط سفلي"). و`withPageView: false` مُمرَّرة فعلاً لـ`QuranPagesScreen` — تُعطّل تحديداً السحب الثقيل (`PageView`) المذكور في هذا البند. الأرجح أن هذا حُلّ ضمن جلسة الانتقال لحزمة `quran_library` ولم يُحدَّث هذا السجل حينها. **لم يُتحقّق بصرياً حياً في هذه الجلسة** (تعذّر أخذ لقطة شاشة فعلية لتطبيق سطح المكتب في هذه البيئة المعزولة دون إضعاف إعداد أمني — رُفض عمداً)؛ التوصية: تأكيد بصري سريع من مطوّر بشري قبل إغلاق هذا البند نهائياً، لكن لا يوجد أي دليل برمجي على استمرار المشكلة |
| TD-11 | ~~اسم السورة مشوّه في ترويسة وضع الصفحات (يُعرض بخط HafsSmart وهو نص عادي)~~ **[على الأرجح محلول ضمنياً — تحقق 2026-07-15]** | ~~متوسط~~ | 🟡 | — | — | — | **تحقق فعلي من الكود**: خط "HafsSmart" غير مستخدم إطلاقاً في `page_reader_screen.dart` الحالي (مؤكَّد عبر `git log -S "HafsSmart"` — آخر ظهور له كان في commit سابق لهجرة حزمة `quran_library`، وحصراً في `mushaf_page_datasource.dart`، ملف مصدر بيانات منفصل غير مستخدم من قِبل شاشة وضع الصفحات الحالية). اسم السورة في وضع الصفحات الحالي يُعرض عبر `SurahHeaderWidget` المدمج في حزمة `quran_library` نفسها، الذي يستخدم خط عائلة `surahName` (`surah_name_naskh.ttf`) المُعرَّف بشكل صحيح في `pubspec.yaml` الخاص بالحزمة والموجود فعلياً كملف (283KB) — وليس أي نص عادي. `SurahNameStyle` في الحزمة لا يعرض حتى معامل خط (لون/حجم فقط)، فلا مجال لأن يفرض كود سراج خط HafsSmart هنا. **لم يُتحقّق بصرياً حياً** لنفس سبب TD-10 أعلاه — العلامة 🟡 بدل ✅ تحديداً لأن هذا بند "مظهر" (تشوّه بصري) يصعب تأكيده 100% من الكود وحده مقارنة بـTD-10 (سلوك/تفاعل يمكن تتبعه منطقياً بثقة أعلى). التوصية: تأكيد بصري بشري سريع (فتح وضع الصفحات على صفحة تبدأ سورة جديدة) |
| TD-12 | audioplayers للراديو (لا خلفية سليمة/foreground service) | متوسط | P1 | متوسطة–عالية | 1–2 يوم | الراديو يتوقف بإقفال الشاشة + متطلب Android 14 | الانتقال لـ just_audio + خدمة أمامية (مجدول أصلاً) |
| TD-13 | ~~لا CI~~ **[محلول 2026-07-10]** | ~~عالٍ~~ | ✅ | — | — | — | GitHub Actions فعّال (analyze+test)، أول تشغيل ناجح بعد 3 محاولات كشفت وأصلحت: ملف اختبار ميت (widget_test.dart)، سلوك flutter analyze الدقيق في بيئة نظيفة، خطأ صياغي في سكربت. راجع .github/workflows/ci.yml |
| TD-14 | إعدادات android/ios لم تُراجع (أذونات/توقيع/targetSdk) — غير متحقق | مجهول | P0 كشفاً | سهلة | ساعات | مفاجآت رفض متاجر | جلسة جرد كاملة للملفين مع توثيق كل إذن وسببه |
| TD-16 | ~~RLS معطّل على 8 جداول Supabase (asbab_al_nuzul, ayah_hadiths, ayahs, children_stories, hadiths, stories, tafsir, word_meanings)~~ **[محلول 2026-07-10]** | ~~حرج جداً~~ | ✅ | — | — | كان: أي حامل anonKey يقرأ/يعدّل/يحذف كل المحتوى الديني مباشرة، متجاوزاً التطبيق كلياً | حُلّ عبر ALTER TABLE...ENABLE ROW LEVEL SECURITY + سياسة "Public read access" (SELECT فقط) على كل جدول من لوحة Supabase SQL Editor. تحقق بصري: القراءة تعمل (شاشة تفسير)، لا كسر وظيفي |
| TD-16 | ~~RLS معطّل على 8 جداول Supabase (asbab_al_nuzul, ayah_hadiths, ayahs, children_stories, hadiths, stories, tafsir, word_meanings)~~ **[محلول 2026-07-10]** | ~~حرج جداً~~ | ✅ | — | — | كان: أي حامل anonKey يقرأ/يعدّل/يحذف كل المحتوى الديني مباشرة، متجاوزاً التطبيق كلياً | حُلّ عبر ALTER TABLE...ENABLE ROW LEVEL SECURITY + سياسة "Public read access" (SELECT فقط) على كل جدول من لوحة Supabase SQL Editor. تحقق بصري: القراءة تعمل (شاشة تفسير)، لا كسر وظيفي |
| TD-16 | ~~RLS معطّل على 8 جداول Supabase (asbab_al_nuzul, ayah_hadiths, ayahs, children_stories, hadiths, stories, tafsir, word_meanings)~~ **[محلول 2026-07-10]** | ~~حرج جداً~~ | ✅ | — | — | كان: أي حامل anonKey يقرأ/يعدّل/يحذف كل المحتوى الديني مباشرة، متجاوزاً التطبيق كلياً | حُلّ عبر ALTER TABLE...ENABLE ROW LEVEL SECURITY + سياسة "Public read access" (SELECT فقط) على كل جدول من لوحة Supabase SQL Editor. تحقق بصري: القراءة تعمل (شاشة تفسير)، لا كسر وظيفي |
| TD-15 | صفر توثيق للأصول (من أين جاء كل ملف بيانات، متى، بأي ترخيص) | متوسط | P1 | سهلة | ساعات | فقدان أثر المصادر مع الوقت — قاتل لمشروع محتوى ديني | ملف ASSETS_PROVENANCE.md: كل أصل + مصدره + تاريخه + ترخيصه + كيفية تحديثه |

## ديون مرفوضة عمداً (ليست ديوناً — قرارات)

- عدم بناء Knowledge Engine/Workspaces/لوحة تحكم مخصصة الآن: **قرارات
  معمارية موثّقة بمعايير تفعيل** (SIRAJ_PRINCIPLES)، لا ديون.
- عدم تنفيذ SQLite الآن رغم TD-03: مشروط بالقياس أولاً — منهج سليم.

## بروتوكول السداد

كل جلسة عمل: بند P0 واحد على الأقل من هذا السجل قبل أي ميزة جديدة،
حتى يخلو السجل من P0. البنود P2 تُجمع في "جلسات تنظيف" دورية مخصصة.
