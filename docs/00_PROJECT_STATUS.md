# 00 — حالة مشروع سراج (Project Status)

> **المرجع الرسمي للمشروع.** هذه الوثيقة نقطة الدخول: تلخّص أين نقف الآن،
> وتحيل لبقية وثائق الـ Playbook. تُحدَّث مع كل تغيير جوهري.
>
> آخر تحديث: يوليو 2026.
>
> **قاعدة التحقق:** لا يُعتبر أي بند "مكتملاً" هنا إلا إذا تُحقق منه فعلياً
> (بناء مشترك موثّق في الجلسات، أو فحص بصري، أو اختبار قطع إنترنت).
> ما لم يُتحقق منه يُكتب صراحة: **غير متحقق — يحتاج مراجعة فعلية للمستودع.**

---

## هوية المشروع

- **الاسم:** سراج (Siraj) — منصة معرفة إسلامية شاملة، خيرية بالكامل (لا أرباح، لا إعلانات، لا تتبّع).
- **التقنيات:** Flutter 3.44.x + Riverpod 3 (Notifier) + GoRouter + Supabase + Firebase Auth + Hive.
- **الفلسفة الحاكمة:** `SIRAJ_PRINCIPLES.md` — "Product with Platform Boundaries" + Offline-first.
- **اللغات:** 15 لغة كاملة (ar, en, ur, fa, id, tr, fr, bn, ms, ha, sw, de, ru, zh, es) مع RTL/LTR.
- **المستودع:** github.com/m-alajmi-eng/Siraj (خاص). متعاون: علاء (Write).

## خريطة وثائق الـ Playbook

| # | الوثيقة | الغرض |
|---|---|---|
| 00 | PROJECT_STATUS | أنت هنا — الحالة والملخص |
| 01 | ENGINEERING_REVIEW | المراجعة الهندسية الشاملة (لجنة متعددة التخصصات) |
| 02 | STORE_COMPLIANCE | امتثال المتاجر (Apple/Google/Huawei/Microsoft/سطح المكتب/Web) |
| 03 | MASTER_CHECKLIST | القائمة الرئيسية الشاملة — أهم وثيقة تنفيذية |
| 04 | RELEASE_PLAN | خطة الإطلاق Alpha→Production بمعايير انتقال |
| 05 | TECH_DEBT | سجل الديون التقنية |
| 06 | RISK_REGISTER | سجل المخاطر |

**وثائق قائمة تبقى سارية (لا تُلغى):** `SIRAJ_PRINCIPLES.md`،
`KHATMAH_DESIGN.md`، `QURAN_SECTION_CHECKLIST.md`، `ROADMAP.md`،
`NEXT_SESSION_PLAN.md`، `PLATFORM_DASHBOARD_PLAN.md`، `adr/ADR-001..007`.
هذا الـ Playbook طبقة تنظيمية فوقها، لا بديل عنها.

---

## ملخص الحالة الآن (متحقق من جلسات البناء الفعلية)

### ✅ مكتمل ومتحقق بصرياً
- **الاستقلالية المحلية (ADR-006، المراحل 1–4):** نص القرآن العثماني (604 صفحة)
  + 14 ترجمة + التفسير الميسّر + التجويد الملوّن + أوقات الصلاة + 8 أصوات أذان —
  كلها محلية، مربوطة فعلياً وقت التشغيل، ومختبرة **بقطع الإنترنت**.
- **الختمة (مرحلتا 1–2 من 7):** نموذج `KhatmahPlan` كامل بالحسابات المشتقّة +
  تخزين Hive + `KhatmahNotifier` + شاشتا القائمة والإنشاء + 21 مفتاح ترجمة ×15 لغة.
- **خطوط رسمية محسومة الترخيص:** فحصنا EULA المضمّن فعلياً داخل ملفات
  KFGQPC — "Use, Copy, Distribute" مجاناً، ممنوع البيع/التعديل. `HafsSmart_08.ttf`
  مسجّل في pubspec ويعرض بنجاح (تحقق بصري بالصور).
- **بقية الأقسام العاملة:** أذكار، مكتبة (IslamHouse)، بوابة غير المسلمين،
  راديو، قبلة، إعدادات، onboarding.

### 🟡 قيد العمل (جزئي)
- **وضع صفحات المصحف (`PageReaderScreen`):** هذه الفقرة كانت تصف نسخة
  سابقة على هجرة حزمة `quran_library` ولم تُحدَّث حينها. **تحديث
  2026-07-15** (تحقق فعلي من الكود، راجع `05_TECH_DEBT.md` TD-10/TD-11
  للتفاصيل الكاملة): النسخة الحالية تستخدم `QuranPagesScreen` من
  `quran_library` (line-locked مطابق للمطبوع فعلياً)، ولا تستخدم خط
  HafsSmart إطلاقاً. أزرار التنقّل (`chevron_right/left` + `_goToPage`)
  موجودة وفعّالة. اسم السورة يُعرض بخط الحزمة الصحيح `surahName` لا
  بخط خاطئ. لم يتحقق تأكيد بصري حيّ في هذه الجلسة (بيئة معزولة بلا أداة
  التقاط شاشة آمنة) — تأكيد بشري سريع متبقٍّ قبل اعتبار البند ✅ نهائياً.
  القرار المعماري النهائي لمصدر التخطيط موثّق في `QURAN_SECTION_CHECKLIST.md`.
- **الختمة (مراحل 3–7):** شاشة التفاصيل، التذكيرات، توحيد متابعة القراءة، المشاركة.

### ❌ محظورات إطلاق مفتوحة (راجع 03 و06)
1. **البناء الإنتاجي معطّل** — Gradle 9.1.0 × Kotlin 2.3.20 (متحقق بالتجربة).
2. **صفر اختبارات مكتوبة** خلال كل الجلسات. (وجود `test/widget_test.dart`
   الافتراضي: غير متحقق — يحتاج مراجعة فعلية للمستودع.)
3. **لا سياسة خصوصية / لا حذف حساب / لا Crashlytics.**
4. **تراخيص محتوى غير محسومة:** الراديو، IslamHouse، بيانات التجويد،
   ونص الشهادة معلَّم `needs_verification`.

### غير متحقق — يحتاج مراجعة فعلية للمستودع
إعدادات `android/` و`ios/` الكاملة، وجود مجلد اختبارات، طريقة تخزين مفاتيح
Supabase/Firebase في الكود، تفعيل RLS على جداول Supabase، وجود أي CI،
`.gitignore` وملفات الأسرار.

---

## أعلى 6 أولويات (بالترتيب)

1. إصلاح البناء الإنتاجي (Gradle/Kotlin) — بوابة كل شيء.
2. GitHub Actions (تحليل + اختبار + بناء APK سحابياً) — يحل أيضاً هشاشة جهاز التطوير (7.4GB RAM، تجمّد مرتين).
3. إنهاء المصحف + الختمة وفق `NEXT_SESSION_PLAN.md`.
4. حزمة الامتثال: سياسة خصوصية + حذف حساب + Crashlytics.
5. أساس الاختبارات: `KhatmahPlan` + ثوابت البيانات الدينية (6236 آية / 604 صفحة / 114 سورة).
6. حسم تراخيص المحتوى المعلّقة (الراديو أولاً).
