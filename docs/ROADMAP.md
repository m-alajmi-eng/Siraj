# خارطة طريق سراج - الميزات المستقبلية

وثيقة حيّة تجمع الميزات المرشّحة، مبنية على بحث تنافسي شامل (يوليو 2026)
لتطبيقات القرآن والحفظ الرائدة (Quran.com، iQuran، Tarteel، Muslim Pro،
Greentech Al Quran، Quran Companion، Noor Ul Quran، وغيرها).

الترتيب داخل كل مجموعة حسب: القيمة للمستخدم ÷ التعقيد. لا يُبنى أي بند
إلا وفق مبادئ سراج (Product بحدود، أتقن واحدة بعمق، قِس قبل التحسين).

---

## نقاط قوة سراج الحالية (ما يميّزنا عن المنافسين)
- 15 لغة كاملة (معظم المنافسين أقل)
- بوابة غير المسلمين (فريدة)
- المكتبة الشاملة (IslamHouse، 7 أقسام)
- استقلالية محلية كاملة (قرآن، تجويد، ترجمات، أذان، أوقات صلاة)
- التجويد الملوّن المحلي
- بلا إعلانات، بلا تتبّع، خيري

---

## المجموعة أ: قبل الإطلاق (أساسية)

| # | الميزة | التعقيد | ملاحظة |
|---|--------|---------|--------|
| أ1 | توحيد "متابعة القراءة" (نقطة دخول واحدة من الرئيسية وقسم القرآن لآخر موضع) | منخفض | مشكلة حالية مؤكّدة |
| أ2 | Firebase Crashlytics + Analytics (حد أدنى محترم للخصوصية) | منخفض | قرار ADR سابق |
| أ3 | تأمين رموز Auth في flutter_secure_storage | منخفض | حالياً Hive عادي |
| أ4 | تعتيم الكود عند بناء الإصدار (--obfuscate) | منخفض | سطر أمر واحد |
| أ5 | التحقق من نص الشهادة المعلّق (needs_verification) | منخفض | من جلسات سابقة |

---

## المجموعة ب: تجربة القراءة

| # | الميزة | التعقيد | ملاحظة |
|---|--------|---------|--------|
| ب1 | زر تبديل الوضع (محتوى كامل ↔ light/بسيط) | منخفض | طلب مستخدم |
| ب2 | أحجام خط قابلة للتخصيص + ألوان مخصصة (نص/ترجمة/تفسير) | منخفض | معيار شائع |
| ب3 | مخطط الختمة (Khatmah Planner): تقسيم القرآن على أيام + هدف يومي + تذكير + تتبّع "على المسار/متأخر" | متوسط | ميزة قوية جداً عند المنافسين |
| ب4 | عرض متعدد الصفحات (صفحات في شاشة واحدة لسرعة الختمة) | متوسط | طلب مستخدم مباشر |
| ب5 | تخطيط ثابت الأسطر (line-locked، مصحف المدينة 15 سطر) | متوسط-عالٍ | حاسم للحفّاظ (recall بصري) |
| ب6 | خطوط مصحف متعددة (Madani/Warsh/Qaloon/Indopak) | متوسط | معيار شائع |
| ب7 | إحساس تقليب الصفحة (page-flip) | متوسط | تحسين تجربة |
| ب8 | Autoscroll (تمرير تلقائي بسرعة قابلة للضبط) | منخفض | شائع |

---

## المجموعة ج: الصوت والتلاوة

| # | الميزة | التعقيد | ملاحظة |
|---|--------|---------|--------|
| ج1 | تشغيل خلفي + تحكم خارجي (إشعار/شاشة قفل/سماعات) | عالٍ | طلب مستخدم؛ يتطلب الانتقال audioplayers→just_audio |
| ج2 | تتبّع بصري متزامن (إضاءة كل كلمة/آية مع التلاوة) | متوسط-عالٍ | ميزة محبوبة جداً |
| ج3 | تكرار آية/نطاق (A-B repeat) بعدد وسرعة قابلين للضبط | متوسط | أساسي للحفظ |
| ج4 | تحكم بسرعة التلاوة | منخفض | شائع |
| ج5 | تشغيل ترجمة صوتية بعد كل آية عربية | متوسط | لغير الناطقين بالعربية |

---

## المجموعة د: الفهم

| # | الميزة | التعقيد | ملاحظة |
|---|--------|---------|--------|
| د1 | العرض كلمة بكلمة (ترجمة + جذر + إعراب لكل كلمة، بالنقر) | عالٍ | الأكثر طلباً؛ يرتبط بالمرحلة 5 من ADR-006 (غريب القرآن) والمرحلة 7 (بوابة معرفية) |
| د2 | تفاسير متعددة قابلة للمقارنة جنباً لجنب | متوسط | يرتبط بالمرحلة 3 (تفسير محلي) |
| د3 | ترجمات متعددة جنباً لجنب (حتى 4) | متوسط | معيار Quran.com/Noor |

---

## المجموعة هـ: الحفظ (Hifz) — ميزة كبيرة قائمة بذاتها

| # | الميزة | التعقيد | ملاحظة |
|---|--------|---------|--------|
| هـ1 | تعليم الآيات (محفوظة/قيد مراجعة/تحتاج تقوية) | متوسط | أساس الحفظ |
| هـ2 | نظام التكرار المتباعد (SRS/FSRS) + قوائم مراجعة يومية | عالٍ | معيار ذهبي للحفظ |
| هـ3 | إخفاء النص بعد عدد تكرارات (للحفظ) | متوسط | تقنية حفظ مثبتة |
| هـ4 | اختبار الحفظ (وضع الاختبار، إخفاء آيات) | متوسط | يحاكي المعلّم |
| هـ5 | ربط الآيات تدريجياً (1|2|1,2|3...) | متوسط | تقنية حفظ فعّالة |

ملاحظة: مساعد تصحيح التلاوة بالـAI مؤجَّل صراحةً (ADR-005).

---

## المجموعة و: التتبّع والتحفيز

| # | الميزة | التعقيد | ملاحظة |
|---|--------|---------|--------|
| و1 | streaks (سلاسل أيام) + شارات + إنجازات | متوسط | يحوّل العادة للعبة |
| و2 | إحصائيات ورسوم بيانية للتقدّم | متوسط | تحفيز |
| و3 | رحلات موضوعية (صبر، شكر، رحمة...) | متوسط | ميزة Noor Ul Quran |
| و4 | تتبّع بالجزء + عدّاد ختمات | منخفض | شائع |

---

## المجموعة ز: المزامنة والبيانات

| # | الميزة | التعقيد | ملاحظة |
|---|--------|---------|--------|
| ز1 | مزامنة الموضع/العلامات عبر الأجهزة (اختياري بحساب) | عالٍ | يتطلب RLS + Supabase؛ يراعى مبدأ الخصوصية |
| ز2 | استيراد/تصدير الإعدادات والعلامات | منخفض | بديل خصوصي للمزامنة |
| ز3 | بحث بالكلمة/الجزء/المرجع (2:255) | منخفض-متوسط | معيار أساسي |
| ز4 | علامات مرجعية متعددة + وسوم + مشاركة الآية | منخفض | شائع |

---

## المجموعة ح: ميزات مؤجّلة بقرار واعٍ (لا تُبنى قريباً)
- مساعد تصحيح التلاوة بالـAI (ADR-005 - تكلفة تشغيل GPU)
- ميزات جماعية/اجتماعية (ختمة جماعية، مجموعات قراءة) - تعقيد backend كبير
- قصص الأطفال (ADR-006 مرحلة 6 - ميزة منفصلة مستقبلية)

---

## ملاحظة منهجية
هذه الوثيقة **مرجع**، لا التزام تنفيذ. كل بند يُقيَّم عند وصوله بمعيار:
هل يخدم مستخدماً حاضراً؟ هل قيمته تبرّر تعقيده؟ هل يتوافق مع مبادئنا؟
البنود المرتبطة بخطة ADR-006 (قرآن/تفسير/غريب محلي) تُنفَّذ ضمنها لا مكرّرة.

*آخر تحديث: يوليو 2026 - بعد بحث تنافسي شامل.*

---

## قرار مؤجَّل: توحيد متابعة القراءة + نظام القراءة الجديد (يوليو 2026)

قرار: بند "ب3/ب4" (مخطّط الختمة + عرض صفحات متعددة) و"توحيد متابعة
القراءة" (كانا منفصلين في هذه الوثيقة) **يُدمَجان في مشروع واحد متكامل**،
لا ينفَّذ أي جزء منه منفرداً لتفادي إعادة عمل قريبة.

### التصميم المتفق عليه (بانتظار التنفيذ الكامل):
- **وضعان منفصلان للقراءة:** الوضع الحالي (سورة واحدة) يبقى كما هو؛
  وضع "قراءة الختمة" جديد يعرض صفحات المصحف الحقيقية (604 صفحة،
  البيانات جاهزة فعلاً في quran_uthmani.json حقل `page`).
- **زر تبديل** بين الوضعين في شريط أدوات القارئ، بجانب زر التجويد.
- **مخطّط الختمة:** يدعم خططاً زمنية مجدولة (مثل رمضان 30 يوماً) مع
  حساب صفحات/يوم تلقائي وتذكيرات وتتبّع "على المسار/متأخر".
- **توحيد متابعة القراءة:** مصدر بيانات واحد يُستخدم من الرئيسية وقسم
  القرآن ووضع الختمة معاً، يعرف تلقائياً أي وضع (سورة عادية أو ختمة
  نشطة) هو النشط ويوجّه المستخدم بدقة (بما فيها التمرير الدقيق لموضع
  الآية - مؤجَّل سابقاً لهذا المشروع تحديداً).
- **ملاحظة تصميم متجاوب:** عند بناء الواجهات، مراعاة الشاشات الأكبر
  (iPad) بعرض/خط/تخطيط مختلف (ربما صفحتين جنباً لجنب في وضع الختمة).

### حالة التنفيذ
لم يبدأ العمل الفعلي بعد. محاولة جزئية (إضافة بطاقة "متابعة القراءة"
في quran_home_screen.dart بمعزل عن الباقي) نُفذت ثم **أُلغيت عمداً**
(`git checkout`) بعد أن تبيّن أنها تجزئة غير مفيدة تخالف قرار "لا نبني
مرتين". يُنفَذ هذا المشروع بالكامل دفعة واحدة عند البدء الفعلي به.

---

## قرار مؤجَّل: توحيد زر العودة عبر كل الشاشات (يوليو 2026)

مشكلة مؤكّدة من الاستخدام الفعلي: أزرار العودة غير متّسقة عبر التطبيق:
- بعض الشاشات بلا زر عودة واضح إطلاقاً (لا يمكن الرجوع بسهولة)
- بعضها زر غير واضح (منطقة ضغط صغيرة في الزاوية فقط)
- بعضها زر واضح جيد

الحل المطلوب (ضمن مرحلة تحسين التصميم الشاملة):
- زر عودة موحّد واضح (أيقونة + منطقة ضغط كافية) في كل الشاشات الفرعية
- الأفضل: widget مشترك (مثل SirajBackButton) أو AppBar موحّد يُستخدم
  في كل شاشة تفتح بـ push، يحترم اتجاه RTL/LTR حسب اللغة
- مراجعة كل الشاشات الفرعية (البوابة، المكتبة، الختمة، الإعدادات،
  القبلة، الإحصائيات...) لضمان اتّساق التنقّل والرجوع

يُنفَّذ ضمن مرحلة تحسين تجربة المستخدم/التصميم الموحّد، لا منفرداً.

## قرار مؤجَّل: نظام التلميحات الشامل (Hints/Onboarding) — آخر خطوة قبل الاختبار (يوليو 2026)

**القرار:** بناء نظام تلميحات موحّد (`OneTimeHint` قابل لإعادة الاستخدام
+ تلميحات سياقية لكل الأيقونات/الميزات المهمة عبر التطبيق) يُؤجَّل
عمداً حتى **آخر مرحلة قبل بدء الاختبار الفعلي مع المستخدمين**، لا
يُبنى جزئياً الآن لكل ميزة على حدة.

**السبب:** نظام تلميحات متسق يحتاج رؤية كاملة لكل الشاشات المكتملة
فعلياً؛ بناؤه الآن لميزة واحدة (الوضع الخفيف/تخصيص الأقسام) يعني
إعادة تصميمه لاحقاً بأسلوب مختلف لبقية الميزات — عمل مكرر.

**نطاق التنفيذ المستقبلي (عند الوصول لهذه المرحلة):**
- مكوّن `OneTimeHint` عام: نص + أيقونة + مفتاح تخزين فريد في Hive
  (`hint_shown_<id>`) + زر "فهمت" يُخفيه نهائياً بعد أول ظهور.
- تلميحات مطلوبة (قائمة أولية، تُراجَع وتُكمَّل عند الوصول للمرحلة):
  - الوضع الخفيف/الكامل: شرح الفرق + "اذهب للإعدادات لمزيد من التخصيص".
  - زر تبديل الوضع في الرئيسية (سيُبنى - راجع قرار أدناه).
  - أيقونات الشريط السفلي الرئيسية عند أول فتح لكل قسم.
  - وضع الصفحات في القرآن (زر التبديل سورة/صفحات، زر التجويد).
  - شاشة الختمة (زر المشاركة، شريط التقدّم).
- شرح مبسّط لكل ميزة رئيسية ضمن نفس النظام (لا نص طويل — سطر أو
  سطرين لكل تلميح، بروح "اكتشاف تدريجي" لا "دليل مستخدم").

**الحالة:** غير مُنفَّذ - يُنفَّذ بالكامل كخطوة واحدة موحّدة في
المرحلة المحدَّدة أعلاه.

## رؤية مستقبلية موثّقة: محرك السياق الزمني-المكاني (بعد المرحلة الحالية)

**التقييم:** الأفضل من بين 5 وثائق معمارية خارجية رُوجعت (يوليو 2026):
مراجعتان بالعربية والإنجليزية لـ"Spatio-Temporal Context Engine"،
ووثيقتا "Living Mind Maps / Knowledge Distillation Engine". اختير
هذا الاتجاه تحديداً لأنه **الأكثر أماناً دينياً والأقوى تسويقياً**:

- الخرائط الذهنية المتولّدة آلياً (الوثيقتان الأخريان) رُفضتا: تعتمدان
  على "محرك ترجيح" يقرر آلياً أي علاقة دينية "أهم" من أخرى بلا حكَم
  بشري واضح لكل قرار - مخاطرة دينية حقيقية (خط أحمر مشابه لـR-01).
- الخريطة الزمنية-المكانية: حقائق جغرافية/تاريخية أكثر ثباتاً،
  وتُبهر المستخدم فوراً بلا حاجة لشرح مفهوم معقّد (معيار wow effect
  حقيقي: يُفهَم بصرياً في ثوانٍ، لا يحتاج فقرة تفسير).

### الفكرة الجوهرية

آية تذكر حدثاً تاريخياً (غزوة، هجرة) → المستخدم يضغط زرا → تظهر
خريطة تفاعلية حقيقية (لا صورة ثابتة) للموقع + خط زمني لمتى حدث +
شخصيات/أحداث مرتبطة. طبقة عرض إضافية فوق بوابة الآيات (qke) الحالية
- لا بديلاً عنها. qke اليوم يجيب "ماذا تعني الآية؟"؛ هذه الطبقة تجيب
"أين ومتى حدث ما تصفه الآية فعلياً؟" - سؤال مختلف تماماً ومكمّل.

### الحقيقة الصادقة بعد البحث الفعلي (مهم جداً - تصحيح توقعات)

بحث ويب فعلي (يوليو 2026) كشف: **لا يوجد مصدر GIS معتمد وموحّد جاهز
للتنزيل** لمواقع الغزوات/الأحداث التاريخية الإسلامية. حتى المواقع
المتخصصة (IslamicLandmarks.com) تصرّح صراحة: "بعض المواقع لا يمكن
التحقق منها بيقين... في بعض الحالات يدّعي أكثر من موقع استضافة نفس
الحدث التاريخي... نعرض الموقع الأكثر قبولاً بين المؤرخين. والله أعلم".

هذا يعني: هذا **ليس مشروع استيراد API بسيطاً** (كما بدا في الوثائق
الأصلية)، بل **مشروع بحثي-تاريخي حقيقي** يحتاج:
- قراءة مصادر موثوقة (كتب سيرة معتمدة، مقالات تاريخية محكّمة) وتحديد
  الإحداثيات يدوياً بدقة معقولة - لا اختراعاً ولا تخميناً آلياً.
- عرض صريح للمستخدم بلغة "الموقع الأكثر قبولاً بين المؤرخين" لا
  "الموقع الحقيقي بيقين مطلق" - أمانة علمية إلزامية.
- على الأرجح استشارة مؤرخ/باحث سيرة، لا مبرمجاً فقط.

### القرار المعماري الموصى به (من المراجعتين الخارجيتين + تعديل)

- **لا** SpatiaLite داخل التطبيق (تعقيد بناء عابر للمنصات بلا داعٍ
  حقيقي - الآية معروفة مسبقاً، لا حاجة لاستعلامات مكانية معقدة).
- **نعم** SQLite (بيانات الأحداث) + ملفات GeoJSON منفصلة (هندسة كل
  حدث، تُحمَّل عند الطلب فقط) + MBTiles (خرائط متجهية، لا صور نقطية
  ثقيلة) - يحافظ على مبدأ خفة التحميل الموثّق سابقاً.
- نموذج الزمن: Era + Sequence + Confidence + StartDate/EndDate بدل
  سنة رقمية جامدة (يعكس عدم اليقين التاريخي بأمانة).
- Camera Controller مخصص (FlyTo/FitBounds/Highlight) بدل FlyTo دائماً.
- البيانات الجغرافية والزمنية منفصلة تماماً عن منطق qke الحالي؛
  تربَط لاحقاً كتبويب إضافي في بوابة الآية، لا إعادة هيكلة لـqke.

### ترتيب التنفيذ الموصى به (لا يبدأ إلا بعد المرحلة الحالية)

1. اختيار 2-3 أحداث فقط للتجربة الأولى (بدر، أُحد، الهجرة) - لا محرك
   شامل من اليوم الأول.
2. بحث تاريخي دقيق لكل حدث + توثيق المصدر + صياغة "الأكثر قبولاً"
   حيثما وُجد خلاف بين المؤرخين.
3. بناء GeoJSON يدوي لكل حدث (نقطة/مسار/منطقة حسب طبيعته - غزوة بدر
   ليست نقطة واحدة بل معسكرات ومسير وميدان اشتباك).
4. طبقة عرض بسيطة (خريطة + خط زمني) كتبويب إضافي في verse_portal_screen
   لهذه الأحداث الثلاثة فقط.
5. تقييم واختبار مستخدمين حقيقي قبل التوسّع لأي حدث إضافي.

**الحالة:** موثّق كرؤية استراتيجية، غير مُنفَذ - يُستأنف بعد إغلاق
المرحلة الحالية (فحص SIRAAJ_* المتبقي + الفحص الشامل النهائي قبل
الإطلاق).

## ملخص جلسة عمل موسّعة (يوليو 2026) — أسباب النزول + قسم الأحاديث الكامل

### ما أُنجز فعلياً هذه الجلسة (كله مدفوع بالـGit، تحقق من التاريخ لمزيد من التفاصيل)

**1. أسباب النزول — مكتمل بالكامل وشغّال في qke:**
- استيراد 201 سجل أسباب نزول من Tafsir MCP (مصدر موثوق: مركز تفسير للدراسات القرآنية،
  إسناد كامل موثّق) إلى جدول `asbab_al_nuzul` الموجود مسبقاً.
- ربط برمجي كان جاهزاً مسبقاً في qke_repository.dart وverse_portal_screen.dart -
  لم يحتج تعديل كود، فقط البيانات كانت ناقصة.
- أصلحنا مشكلتي RTL حقيقيتين موثّقتين بالتفصيل في تاريخ Git:
  - `\r\n` في نص event_text (تنظيف عبر SQL REPLACE).
  - `\u200c` (ZWNJ) في word_meanings.meaning_ar (نفس الحل).
  - CrossAxisAlignment.end -> .stretch في _WordsPage (مشكلة موثّقة رسمياً في
    GitHub Issue #4566 لـFlutter نفسها - Text القصير يتجاهل textAlign/textDirection
    إن لم يُجبَر على امتلاء العرض الكامل).
  - Directionality مفقودة في CitationBadge + إصلاح ترتيب عناصرها بعد الإصلاح.
- أصلحنا خللاً تصميمياً قديماً منفصلاً: تبويب "التفاسير بالعربية" في qke كان معطَلاً
  تماماً (يبحث عن tafsir.sourceId == 'arabic_tafsir' وهو معرّف لا يطابق أيا من
  الستة الحقيقيين: tabari-ar، ibn-kathir-ar، baghawi-ar، saadi-ar، muyassar-ar،
  mukhtasar-ar) - بُني _ArabicTafsirsPage جديد يجمّعهم في قائمة قابلة للتوسيع.

**2. قسم الأحاديث — مبني بالكامل كميزة مستقلة (تصفح موضوعي، لا مرتبط بآية بعد):**
- مصدر البيانات: HadeethEnc.com (islamhouse-dev API الرسمي) - مصدر موثوق، مراجَع
  من IslamHouse. الترخيص: عدم تعديل + إشارة للمصدر (نلتزم به عبر عمود source).
- استُوردت **3571 حديثاً فريداً** بنص كامل + درجة (صحيح/حسن/إلخ) + تخريج + شرح تفصيلي،
  موزَّعة على **7 فئات موضوعية** (القرآن وعلومه 197، الحديث وعلومه 16، العقيدة 725،
  الفقه وأصوله 1820، الفضائل والآداب 1022، الدعوة والحسبة 140، السيرة والتاريخ 353 -
  إجمالي 4257 قبل إزالة التكرار، فحديث واحد كثيراً ما ينتمي لعدة فئات معاً).
- الهيكلة (3 جداول): hadith_categories (7 فئات) + hadiths (3571 حديث فريد، عمود
  category_id لم يعد يُستخدَم مباشرة) + hadith_category_links (جدول ربط كثير-لكثير،
  4257 رابطاً - يحفظ كل انتماء حديث لكل فئاته الحقيقية بلا فقدان بيانات).
  **درس مهم موثَّق:** أول محاولة استخدمت category_id مباشرة على hadiths، ففشلت
  بصمت (upsert استبدل الفئة القديمة بدل إضافة جديدة) لأن hadith واحد قد ينتمي
  لعدة فئات - اكتُشف عبر تحليل تقاطع المعرّفات بين ملفات JSON المصدرية، لا تخميناً.
- Flutter: HadithRepository + hadithCategoriesProvider + hadithsByCategoryProvider +
  HadithCategoriesScreen (قائمة الفئات) + HadithListScreen (ExpansionTile لكل حديث:
  عنوان دائماً ظاهر، نص+درجة+تخريج+شرح عند التوسيع). مسارات: /more/hadith-categories
  و/more/hadith/:categoryId. زر "الأحاديث" مُفعَل في MoreScreen (flags.showHadith
  كان موجودا من نظام الوضعين، لم يكن مستخدَماً في أي بطاقة فعلية بعد).

### الحالة الحالية الدقيقة (مهم لفهم الفجوة المتبقية)

- ✅ قسم "أحاديث" مستقل يعمل: المزيد -> الأحاديث -> فئة -> قائمة أحاديث كاملة.
- ⚠️ **لم يُضَف بعد لشبكة أيقونات الرئيسية (_QuickActions)** - طلب المستخدم صراحة،
  لم يُنفَّذ - أول شيء يُفعَل في الجلسة القادمة.
- ⚠️ تبويب "أحاديث" داخل بوابة الآية (qke) **لا يزال comingSoon: true عمداً** -
  لأنه مصمَّم لعرض أحاديث *مرتبطة بتلك الآية تحديداً* عبر جدول ayah_hadiths، ولا
  يوجد ربط آية<->حديث حقيقي بعد. عرض كل الـ3571 حديثاً بلا تمييز لكل آية سيكون
  مضلِّلاً دينياً (يوحي بربط غير موجود) - قرار واعٍ بعدم الخداع، لا نسيان.

### البحث الشامل في مسار الربط (آية <-> حديث) - لماذا لا يوجد حل جاهز

بحث معمَّق في 3 اتجاهات منفصلة (يوليو 2026)، كلها بلا استثناء أكدت نفس النتيجة:

1. **الأكاديمي العام:** أوراق علمية متعددة (2016-2026) تحاول هذا عبر NLP/embeddings/
   ontologies (AraBERT، cosine similarity، Semantic Hadith ontology). كل الأوراق
   تعترف صراحة أن **التحقق من صحة الروابط لا يزال تحدياً غير محلول**، والحل المقترح
   من الباحثين أنفسهم هو "crowdsourcing" (تحقق بشري جماعي)، لا أتمتة كاملة موثوقة.
2. **مصر (الأزهر):** لا يوجد مشروع تقني علني لربط حديث-آية، فقط خدمات إدارية عامة.
3. **ماليزيا/إندونيسيا:** "Semantic Hadith" (جامعة ماليزية، منشور LDOW 2016، لا يزال
   يستشهَد به كأحدث مرجع بعد 10 سنوات) يقول حرفياً: **"التنفيذ لا يزال يحتاج نضجاً
   أكبر. التحقق من صحة الروابط تحدٍّ كبير. نبحث في نماذج crowdsourcing للتحقق"**.

**الخلاصة القاطعة:** لا توجد قاعدة بيانات ربط حديث-آية جاهزة وموثوقة في أي مكان في
العالم حتى الآن (يوليو 2026) - هذا مجال بحثي عالمي مفتوح فعلياً، لا نقص بحثنا.

### المسار الجديد المُقرَّر للجلسة القادمة: التفسير بالسنة من كتب العلماء مباشرة

بدل انتظار حل تقني جاهز أو بناء AI للتخمين (كلاهما مرفوض لأسباب دينية/تقنية موثّقة
أعلاه)، القرار: **استخراج الربط يدوياً/شبه يدوي من كتب تفسير موثوقة تربط الآية
بالحديث صراحة بإسناد دقيق** - نفس فلسفة أسباب النزول بالضبط (مصدر بشري موثوق،
لا خوارزمية).

**المصدر الأول المُحدَّد: "أضواء البيان في إيضاح القرآن بالقرآن" للشيخ محمد الأمين
الشنقيطي (ت 1393هـ).**
- لماذا هذا الكتاب تحديداً: منهجه المعلَن صراحة (من الكتاب نفسه) هو تفسير القرآن
  بالقرآن أولاً، فإن تعذّر فبالسنة الصحيحة تحديداً - يذكر كل حديث بمصدره الدقيق
  (مثال موثّق فعلياً في هذا البحث: "صحيح البخاري، كتاب تفسير القرآن، سورة الأنعام،
  رقم 4351" عند تفسير آية معيّنة) - إسناد دقيق قابل للتحقق والاستخراج المنهجي.
- تنبيه منهجي مهم اكتُشف بالبحث (من مقال نقدي لمركز تفسير للدراسات القرآنية نفسه):
  الكتاب أحياناً يستخدم "روايات إسرائيلية" (مصادر يهودية/مسيحية قديمة) لتفسير بعض
  المبهمات، لا القرآن ولا السنة الصحيحة فقط - **يجب فلترة هذه الحالات عند الاستخراج**
  ولا نُدرجها كـ"حديث نبوي" أبداً - فحص كل رابط يدوياً قبل الإدراج لا نسخاً آلياً أعمى.
- الكتاب متاح رقمياً (نسخ متعددة: إسلام واي مفهرسة، Internet Archive، مواقع تراثية) -
  يحتاج تحميل نسخة موثوقة ومُهيكَلة قدر الإمكان (يُفضَّل نسخة بها فهرسة آيات واضحة).
- **الكتاب لا يغطي كل آيات القرآن** (ذكر صراحة: توقف فقط عند الآيات التي رأى لها
  تفسيراً بالقرآن أو السنة تحديداً) - تغطية جزئية طبيعية ومقبولة، موثَّقة بصدق
  للمستخدم (نفس مبدأ "201 من 6236" في أسباب النزول - ليس كل آية لها سبب نزول أو
  تفسير بالسنة، وهذا لا يقلل من قيمة ما هو موجود).

**خطة العمل المقترَحة للجلسة القادمة (بالترتيب):**
1. إضافة قسم "الأحاديث" لشبكة أيقونات الرئيسية (_QuickActions) - سريع، لم يُنفَّذ.
2. تحميل/تحديد مصدر رقمي موثوق لكتاب "أضواء البيان" (فحص الخيارات: PDF مقابل نص
   مُهيكَل، أيهما أنسب للاستخراج الآلي المبدئي ثم المراجعة اليدوية).
3. استخراج تجريبي لجزء صغير (سورة أو جزء واحد) - نتحقق من جودة الاستخراج وسلامته
   الدينية قبل التوسّع (نفس منهجية "الاختبار على فئة صغيرة أولاً" التي نجحت اليوم
   مع فئة "الحديث وعلومه" الـ16 قبل التوسّع الكامل).
4. بناء verse_hadith_relations (الجدول مصمَّم سابقاً في وثيقة QKE، source_ref
   TEXT NOT NULL بالضبط لهذا الغرض - يُلزم بذكر "أضواء البيان، تفسير آية كذا"
   لكل رابط، لا ربط بلا مصدر أبداً).
5. عند اكتمال جزء معقول: تفعيل تبويب "أحاديث" الحقيقي في بوابة الآية (استبدال
   comingSoon: true بعرض حقيقي - لكن **فقط للآيات التي لها ربط موثّق فعلياً**،
   بقية الآيات تبقى "لا تتوفر أحاديث مرتبطة" بصدق، لا فراغ مضلِّل ولا حديث عشوائي).

**مصادر إضافية محتملة لاحقاً (لم تُفحَص بعمق بعد، تستحق بحثاً في الجلسة القادمة
أو ما بعدها):** كتب تفسير أخرى بنفس منهج "التفسير بالمأثور" (تفسير ابن كثير نفسه -
موجود جزئياً بالفعل في qke كتفسير عام، لكن لم يُستخرَج منه رابط آية<->حديث منفصل
بعد؛ تفسير الطبري؛ فتح القدير للشوكاني) - نفس المبدأ: عالِم موثوق يذكر حديثاً
بإسناده عند تفسير آية محددة، قابل للاستخراج بنفس المنهجية.

### تذكير بحالة عامة أخرى (من قرارات سابقة،[200~cd ~/projects/siraj && cat >> docs/ROADMAP.md << 'EOF'

## ملخص جلسة عمل موسّعة (يوليو 2026) — أسباب النزول + قسم الأحاديث الكامل

### ما أُنجز فعلياً هذه الجلسة (كله مدفوع بالـGit، تحقق من التاريخ لمزيد من التفاصيل)

**1. أسباب النزول — مكتمل بالكامل وشغّال في qke:**
- استيراد 201 سجل أسباب نزول من Tafsir MCP (مصدر موثوق: مركز تفسير للدراسات القرآنية،
  إسناد كامل موثّق) إلى جدول `asbab_al_nuzul` الموجود مسبقاً.
- ربط برمجي كان جاهزاً مسبقاً في qke_repository.dart وverse_portal_screen.dart -
  لم يحتج تعديل كود، فقط البيانات كانت ناقصة.
- أصلحنا مشكلتي RTL حقيقيتين موثّقتين بالتفصيل في تاريخ Git:
  - `\r\n` في نص event_text (تنظيف عبر SQL REPLACE).
  - `\u200c` (ZWNJ) في word_meanings.meaning_ar (نفس الحل).
  - CrossAxisAlignment.end -> .stretch في _WordsPage (مشكلة موثّقة رسمياً في
    GitHub Issue #4566 لـFlutter نفسها - Text القصير يتجاهل textAlign/textDirection
    إن لم يُجبَر على امتلاء العرض الكامل).
  - Directionality مفقودة في CitationBadge + إصلاح ترتيب عناصرها بعد الإصلاح.
- أصلحنا خللاً تصميمياً قديماً منفصلاً: تبويب "التفاسير بالعربية" في qke كان معطَلاً
  تماماً (يبحث عن tafsir.sourceId == 'arabic_tafsir' وهو معرّف لا يطابق أيا من
  الستة الحقيقيين: tabari-ar، ibn-kathir-ar، baghawi-ar، saadi-ar، muyassar-ar،
  mukhtasar-ar) - بُني _ArabicTafsirsPage جديد يجمّعهم في قائمة قابلة للتوسيع.

**2. قسم الأحاديث — مبني بالكامل كميزة مستقلة (تصفح موضوعي، لا مرتبط بآية بعد):**
- مصدر البيانات: HadeethEnc.com (islamhouse-dev API الرسمي) - مصدر موثوق، مراجَع
  من IslamHouse. الترخيص: عدم تعديل + إشارة للمصدر (نلتزم به عبر عمود source).
- استُوردت **3571 حديثاً فريداً** بنص كامل + درجة (صحيح/حسن/إلخ) + تخريج + شرح تفصيلي،
  موزَّعة على **7 فئات موضوعية** (القرآن وعلومه 197، الحديث وعلومه 16، العقيدة 725،
  الفقه وأصوله 1820، الفضائل والآداب 1022، الدعوة والحسبة 140، السيرة والتاريخ 353 -
  إجمالي 4257 قبل إزالة التكرار، فحديث واحد كثيراً ما ينتمي لعدة فئات معاً).
- الهيكلة (3 جداول): hadith_categories (7 فئات) + hadiths (3571 حديث فريد، عمود
  category_id لم يعد يُستخدَم مباشرة) + hadith_category_links (جدول ربط كثير-لكثير،
  4257 رابطاً - يحفظ كل انتماء حديث لكل فئاته الحقيقية بلا فقدان بيانات).
  **درس مهم موثَّق:** أول محاولة استخدمت category_id مباشرة على hadiths، ففشلت
  بصمت (upsert استبدل الفئة القديمة بدل إضافة جديدة) لأن hadith واحد قد ينتمي
  لعدة فئات - اكتُشف عبر تحليل تقاطع المعرّفات بين ملفات JSON المصدرية، لا تخميناً.
- Flutter: HadithRepository + hadithCategoriesProvider + hadithsByCategoryProvider +
  HadithCategoriesScreen (قائمة الفئات) + HadithListScreen (ExpansionTile لكل حديث:
  عنوان دائماً ظاهر، نص+درجة+تخريج+شرح عند التوسيع). مسارات: /more/hadith-categories
  و/more/hadith/:categoryId. زر "الأحاديث" مُفعَل في MoreScreen (flags.showHadith
  كان موجودا من نظام الوضعين، لم يكن مستخدَماً في أي بطاقة فعلية بعد).

### الحالة الحالية الدقيقة (مهم لفهم الفجوة المتبقية)

- ✅ قسم "أحاديث" مستقل يعمل: المزيد -> الأحاديث -> فئة -> قائمة أحاديث كاملة.
- ⚠️ **لم يُضَف بعد لشبكة أيقونات الرئيسية (_QuickActions)** - طلب المستخدم صراحة،
  لم يُنفَّذ - أول شيء يُفعَل في الجلسة القادمة.
- ⚠️ تبويب "أحاديث" داخل بوابة الآية (qke) **لا يزال comingSoon: true عمداً** -
  لأنه مصمَّم لعرض أحاديث *مرتبطة بتلك الآية تحديداً* عبر جدول ayah_hadiths، ولا
  يوجد ربط آية<->حديث حقيقي بعد. عرض كل الـ3571 حديثاً بلا تمييز لكل آية سيكون
  مضلِّلاً دينياً (يوحي بربط غير موجود) - قرار واعٍ بعدم الخداع، لا نسيان.

### البحث الشامل في مسار الربط (آية <-> حديث) - لماذا لا يوجد حل جاهز

بحث معمَّق في 3 اتجاهات منفصلة (يوليو 2026)، كلها بلا استثناء أكدت نفس النتيجة:

1. **الأكاديمي العام:** أوراق علمية متعددة (2016-2026) تحاول هذا عبر NLP/embeddings/
   ontologies (AraBERT، cosine similarity، Semantic Hadith ontology). كل الأوراق
   تعترف صراحة أن **التحقق من صحة الروابط لا يزال تحدياً غير محلول**، والحل المقترح
   من الباحثين أنفسهم هو "crowdsourcing" (تحقق بشري جماعي)، لا أتمتة كاملة موثوقة.
2. **مصر (الأزهر):** لا يوجد مشروع تقني علني لربط حديث-آية، فقط خدمات إدارية عامة.
3. **ماليزيا/إندونيسيا:** "Semantic Hadith" (جامعة ماليزية، منشور LDOW 2016، لا يزال
   يستشهَد به كأحدث مرجع بعد 10 سنوات) يقول حرفياً: **"التنفيذ لا يزال يحتاج نضجاً
   أكبر. التحقق من صحة الروابط تحدٍّ كبير. نبحث في نماذج crowdsourcing للتحقق"**.

**الخلاصة القاطعة:** لا توجد قاعدة بيانات ربط حديث-آية جاهزة وموثوقة في أي مكان في
العالم حتى الآن (يوليو 2026) - هذا مجال بحثي عالمي مفتوح فعلياً، لا نقص بحثنا.

### المسار الجديد المُقرَّر للجلسة القادمة: التفسير بالسنة من كتب العلماء مباشرة

بدل انتظار حل تقني جاهز أو بناء AI للتخمين (كلاهما مرفوض لأسباب دينية/تقنية موثّقة
أعلاه)، القرار: **استخراج الربط يدوياً/شبه يدوي من كتب تفسير موثوقة تربط الآية
بالحديث صراحة بإسناد دقيق** - نفس فلسفة أسباب النزول بالضبط (مصدر بشري موثوق،
لا خوارزمية).

**المصدر الأول المُحدَّد: "أضواء البيان في إيضاح القرآن بالقرآن" للشيخ محمد الأمين
الشنقيطي (ت 1393هـ).**
- لماذا هذا الكتاب تحديداً: منهجه المعلَن صراحة (من الكتاب نفسه) هو تفسير القرآن
  بالقرآن أولاً، فإن تعذّر فبالسنة الصحيحة تحديداً - يذكر كل حديث بمصدره الدقيق
  (مثال موثّق فعلياً في هذا البحث: "صحيح البخاري، كتاب تفسير القرآن، سورة الأنعام،
  رقم 4351" عند تفسير آية معيّنة) - إسناد دقيق قابل للتحقق والاستخراج المنهجي.
- تنبيه منهجي مهم اكتُشف بالبحث (من مقال نقدي لمركز تفسير للدراسات القرآنية نفسه):
  الكتاب أحياناً يستخدم "روايات إسرائيلية" (مصادر يهودية/مسيحية قديمة) لتفسير بعض
  المبهمات، لا القرآن ولا السنة الصحيحة فقط - **يجب فلترة هذه الحالات عند الاستخراج**
  ولا نُدرجها كـ"حديث نبوي" أبداً - فحص كل رابط يدوياً قبل الإدراج لا نسخاً آلياً أعمى.
- الكتاب متاح رقمياً (نسخ متعددة: إسلام واي مفهرسة، Internet Archive، مواقع تراثية) -
  يحتاج تحميل نسخة موثوقة ومُهيكَلة قدر الإمكان (يُفضَّل نسخة بها فهرسة آيات واضحة).
- **الكتاب لا يغطي كل آيات القرآن** (ذكر صراحة: توقف فقط عند الآيات التي رأى لها
  تفسيراً بالقرآن أو السنة تحديداً) - تغطية جزئية طبيعية ومقبولة، موثَّقة بصدق
  للمستخدم (نفس مبدأ "201 من 6236" في أسباب النزول - ليس كل آية لها سبب نزول أو
  تفسير بالسنة، وهذا لا يقلل من قيمة ما هو موجود).

**خطة العمل المقترَحة للجلسة القادمة (بالترتيب):**
1. إضافة قسم "الأحاديث" لشبكة أيقونات الرئيسية (_QuickActions) - سريع، لم يُنفَّذ.
2. تحميل/تحديد مصدر رقمي � [200~cd ~/projects/siraj && cat >> docs/ROADMAP.md << 'EOF'

## ملخص جلسة عمل موسّعة (يوليو 2026) — أسباب النزول + قسم الأحاديث الكامل

### ما أُنجز فعلياً هذه الجلسة (كله مدفوع بالـGit، تحقق من التاريخ لمزيد من التفاصيل)

**1. أسباب النزول — مكتمل بالكامل وشغّال في qke:**
- استيراد 201 سجل أسباب نزول من Tafsir MCP (مصدر موثوق: مركز تفسير للدراسات القرآنية،
  إسناد كامل موثّق) إلى جدول asbab_al_nuzul الموجود مسبقاً.
- ربط برمجي كان جاهزاً مسبقاً في qke_repository.dart وverse_portal_screen.dart -
  لم يحتج تعديل كود، فقط البيانات كانت ناقصة.
- أصلحنا مشكلتي RTL حقيقيتين موثّقتين بالتفصيل في تاريخ Git:
  - \r\n في نص event_text (تنظيف عبر SQL REPLACE).
  - \u200c (ZWNJ) في word_meanings.meaning_ar (نفس الحل).
  - CrossAxisAlignment.end -> .stretch في _WordsPage (مشكلة موثّقة رسمياً في
    GitHub Issue #4566 لـFlutter نفسها - Text القصير يتجاهل textAlign/textDirection
    إن لم يُجبَر على امتلاء العرض الكامل).
  - Directionality مفقودة في CitationBadge + إصلاح ترتيب عناصرها بعد الإصلاح.
- أصلحنا خللاً تصميمياً قديماً منفصلاً: تبويب "التفاسير بالعربية" في qke كان معطَّلاً
  تماماً (يبحث عن tafsir.sourceId == 'arabic_tafsir' وهو معرّف لا يطابق أياً من
  الستة الحقيقيين: tabari-ar، ibn-kathir-ar، baghawi-ar، saadi-ar، muyassar-ar،
  mukhtasar-ar) - بُني _ArabicTafsirsPage جديد يجمّعهم في قائمة قابلة للتوسيع.

**2. قسم الأحاديث — مبني بالكامل كميزة مستقلة (تصفح موضوعي، لا مرتبط بآية بعد):**
- مصدر البيانات: HadeethEnc.com (islamhouse-dev API الرسمي) - مصدر موثوق، مراجَع
  من IslamHouse. الترخيص: عدم تعديل + إشارة للمصدر (نلتزم به عبر عمود source).
- استُوردت 3571 حديثاً فريداً بنص كامل + درجة (صحيح/حسن/إلخ) + تخريج + شرح تفصيلي،
  موزَّعة على 7 فئات موضوعية (القرآن وعلومه 197، الحديث وعلومه 16، العقيدة 725،
  الفقه وأصوله 1820، الفضائل والآداب 1022، الدعوة والحسبة 140، السيرة والتاريخ 353 -
  إجمالي 4257 قبل إزالة التكرار، فحديث واحد كثيراً ما ينتمي لعدة فئات معاً).
- الهيكلة (3 جداول): hadith_categories (7 فئات) + hadiths (3571 حديث فريد، عمود
  category_id لم يعد يُستخدَم مباشرة) + hadith_category_links (جدول ربط كثير-لكثير،
  4257 رابطاً - يحفظ كل انتماء حديث لكل فئاته الحقيقية بلا فقدان بيانات).
  درس مهم موثَّق: أول محاولة استخدمت category_id مباشرة على hadiths، ففشلت
  بصمت (upsert استبدل الفئة القديمة بدل إضافة جديدة) لأن hadith واحد قد ينتمي
  لعدة فئات - اكتُشف عبر تحليل تقاطع المعرّفات بين ملفات JSON المصدرية، لا تخميناً.
- Flutter: HadithRepository + hadithCategoriesProvider + hadithsByCategoryProvider +
  HadithCategoriesScreen (قائمة الفئات) + HadithListScreen (ExpansionTile لكل حديث:
  عنوان دائماً ظاهر، نص+درجة+تخريج+شرح عند التوسيع). مسارات: /more/hadith-categories
  و/more/hadith/:categoryId. زر "الأحاديث" مُفعَّل في MoreScreen (flags.showHadith
  كان موجوداً من نظام الوضعين، لم يكن مُستخدَماً في أي بطاقة فعلية بعد).

### الحالة الحالية الدقيقة (مهم لفهم الفجوة المتبقية)

- ✅ قسم "أحاديث" مستقل يعمل: المزيد -> الأحاديث -> فئة -> قائمة أحاديث كاملة.
- ⚠️ لم يُضَف بعد لشبكة أيقونات الرئيسية (_QuickActions) - طلب المستخدم صراحة،
  لم يُنفَّذ - أول شيء يُفعَل في الجلسة القادمة.
- ⚠️ تبويب "أحاديث" داخل بوابة الآية (qke) لا يزال comingSoon: true عمداً -
  لأنه مصمَّم لعرض أحاديث مرتبطة بتلك الآية تحديداً عبر جدول ayah_hadiths، ولا
  يوجد ربط آية<->حديث حقيقي بعد. عرض كل الـ3571 حديثاً بلا تمييز لكل آية سيكون
  مضلِّلاً دينياً (يوحي بربط غير موجود) - قرار واعٍ بعدم الخداع، لا نسيان.

### البحث الشامل في مسار الربط (آية <-> حديث) - لماذا لا يوجد حل جاهز

بحث معمَّق في 3 اتجاهات منفصلة (يوليو 2026)، كلها بلا استثناء أكدت نفس النتيجة:

1. الأكاديمي العام: أوراق علمية متعددة (2016-2026) تحاول هذا عبر NLP/embeddings/
   ontologies (AraBERT، cosine similarity، Semantic Hadith ontology). كل الأوراق
   تعترف صراحة أن التحقق من صحة الروابط لا يزال تحدياً غير محلول، والحل المقترح
   من الباحثين أنفسهم هو crowdsourcing (تحقق بشري جماعي)، لا أتمتة كاملة موثوقة.
2. مصر (الأزهر): لا يوجد مشروع تقني علني لربط حديث-آية، فقط خدمات إدارية عامة.
3. ماليزيا/إندونيسيا: Semantic Hadith (جامعة ماليزية، منشور LDOW 2016، لا يزال
   يُستشهَد به كأحدث مرجع بعد 10 سنوات) يقول حرفياً: "التنفيذ لا يزال يحتاج نضجاً
   أكبر. التحقق من صحة الروابط تحدٍّ كبير. نبحث في نماذج crowdsourcing للتحقق".

الخلاصة القاطعة: لا توجد قاعدة بيانات ربط حديث-آية جاهزة وموثوقة في أي مكان في
العالم حتى الآن (يوليو 2026) - هذا مجال بحثي عالمي مفتوح فعلياً، لا نقص بحثنا.

### المسار الجديد المُقرَّر للجلسة القادمة: التفسير بالسنة من كتب العلماء مباشرة

بدل انتظار حل تقني جاهز أو بناء AI للتخمين (كلاهما مرفوض لأسباب دينية/تقنية موثّقة
أعلاه)، القرار: استخراج الربط يدوياً/شبه يدوي من كتب تفسير موثوقة تربط الآية
بالحديث صراحة بإسناد دقيق - نفس فلسفة أسباب النزول بالضبط (مصدر بشري موثوق،
لا خوارزمية).

المصدر الأول المُحدَّد: "أضواء البيان في إيضاح القرآن بالقرآن" للشيخ محمد الأمين
الشنقيطي (ت 1393هـ).
- لماذا هذا الكتاب تحديداً: منهجه المُعلَن صراحة (من الكتاب نفسه) هو تفسير القرآن
  بالقرآن أولاً، فإن تعذّر فبالسنة الصحيحة تحديداً - يذكر كل حديث بمصدره الدقيق
  (مثال موثّق فعلياً في هذا البحث: "صحيح البخاري، كتاب تفسير القرآن، سورة الأنعام،
  رقم 4351" عند تفسير آية معيّنة) - إسناد دقيق قابل للتحقق والاستخراج المنهجي.
- تنبيه منهجي مهم اكتُشف بالبحث (من مقال نقدي لمركز تفسير للدراسات القرآنية نفسه):
  الكتاب أحياناً يستخدم روايات إسرائيلية (مصادر يهودية/مسيحية قديمة) لتفسير بعض
  المبهمات، لا القرآن ولا السنة الصحيحة فقط - يجب فلترة هذه الحالات عند الاستخراج
  ولا ندرجها كـ"حديث نبوي" أبداً - فحص كل رابط يدوياً قبل الإدراج لا نسخاً آلياً أعمى.
- الكتاب متاح رقمياً (نسخ متعددة: إسلام واي مفهرسة، Internet Archive، مواقع تراثية) -
  يحتاج تحميل نسخة موثوقة ومُهيكَلة قدر الإمكان (يُفضَّل نسخة بها فهرسة آيات واضحة).
- الكتاب لا يغطي كل آيات القرآن (ذكر صراحة: توقف فقط عند الآيات التي رأى لها
  تفسيراً بالقرآن أو السنة تحديداً) - تغطية جزئية طبيعية ومقبولة، موثَّقة بصدق
  للمستخدم (نفس مبدأ "201 من 6236" في أسباب النزول).

خطة العمل المقترَحة للجلسة القادمة (بالترتيب):
1. إضافة قسم "الأحاديث" لشبكة أيقونات الرئيسية (_QuickActions) - سريع، لم يُنفَّذ.
2. تحميل/تحديد مصدر رقمي موثوق لكتاب "أضواء البيان".
3. استخراج تجريبي لجزء صغير (سورة أو جزء واحد) - نتحقق من جودة الاستخراج وسلامته
   الدينية قبل التوسّع.
4. بناء verse_hadith_relations (الجدول مصمَّم سابقاً في وثيقة QKE، source_ref
   TEXT NOT NULL بالضبط لهذا الغرض).
5. عند اكتمال جزء معقول: تفعيل تبويب "أحاديث" الحقيقي في بوابة الآية - فقط للآيات
   التي لها ربط موثّق فعلياً، بقية الآيات تبقى "لا تتوفر أحاديث مرتبطة" بصدق.

مصادر إضافية محتملة لاحقاً: تفسير ابن كثير (موجود جزئياً في qke، لم يُستخرَج منه
ربط منفصل بعد)، تفسير الطبري، فتح القدير للشوكاني - نفس منهجية الاستخراج.

### تذكير بحالة عامة أخرى (من قرارات سابقة، لا تزال سارية)

- قصص/سِيَر: لا مصدر بيانات جاهز أبداً - أصعب الأربعة، يبقى آخر أولوية. فئة
  "السيرة والتاريخ" من HadeethEnc (353 حديث) قد تفيد جزئياً هنا لاحقاً.
- نظام التلميحات الشامل: مؤجَّل عمداً لآخر خطوة قبل الاختبار.
- الخريطة الزمنية-المكانية: رؤية مستقبلية موثَّقة، تُستأنف بعد المرحلة الحالية.
- بنود P0 حرجة لم تُفحَص بعد هذه الجلسة:
  - RPC حذف الحساب (delete_user) - لم يُتحقَق من وجوده الفعلي في Supabase بعد.
  - دقة أوقات الصلاة/القبلة مقابل مصدر موثوق - لم تُختبَر فعلياً بعد.
  - اختبار على جهاز Android بطيء حقيقي - كل الاختبار كان على Linux.
  - بقية ملفات SIRAAJ_* (Backend/Database/Flutter Architecture، Design_System،
    AI_Engine، Localization_Spec، Quality_Testing) لم تُفحَص بعد.


## ملخص جلسة عمل (يوليو 2026) — قسم الأحاديث بالرئيسية + حذف الحساب + توسعة حساب الصلاة + استخراج أضواء البيان

### ما أُنجز فعلياً هذه الجلسة (Git commit: 16af76c)

**1. إضافة أيقونة "الحديث" لشبكة الرئيسية — مكتمل:**
- أُضيفت لـ_QuickActions في home_screen.dart، تفتح /more/hadith-categories
  (الأيقونة format_quote_rounded، flags.showHadith، isPush=true).
- كانت قسم الأحاديث (3571 حديث من HadeethEnc) مبنياً بالكامل من جلسة سابقة
  لكن غير مرتبط بشبكة الرئيسية - الآن مرتبط ومُختبَر (hot reload، يفتح صح).

**2. delete_user RPC — مكتمل ومُختبَر فعلياً (بند P0 حرج):**
- اكتُشف عبر SQL Editor أن الدالة لم تكن موجودة إطلاقاً في Supabase رغم
  جهوزية الكود الكامل في Flutter (auth_repository.dart، account_screen.dart،
  ترجمات بـ14 لغة) - الكود ينادي rpc('delete_user') على دالة غير موجودة.
- فحص شامل أكد عدم وجود أي بيانات مستخدم شخصية في Supabase على الإطلاق:
  لا جداول (كل الـ13 جدول محتوى مرجعي عام: قرآن/تفسير/حديث/قصص)، لا
  Storage buckets، لا user_metadata - كل بيانات المستخدم (ختمة، تقدم،
  تفضيلات) محلية بالكامل عبر Hive. هذا بسّط الدالة: حذف auth.users فقط.
- الدالة أُنشئت بـSECURITY DEFINER + WHERE id = auth.uid() (المستخدم
  يحذف حسابه هو فقط، لا يقدر يستهدف مستخدماً آخر حتى لو حاول).
- اختُبرت فعلياً من واجهة التطبيق (لا SQL فقط): حساب تجريبي، حذف من
  الإعدادات، تأكيد اختفائه من auth.users - نجح بالكامل.

**3. دقة أوقات الصلاة - فحص وتصحيح شامل (بند P0):**
- التطبيق يستخدم مكتبة adhan (منفذ Dart لمكتبة Adhan من Batoul Apps،
  معادلات فلكية معتمدة) - لا حساب مخصص يدوي، مطمئن من البداية.
- اكتُشفت مشكلة حقيقية: شاشة الإعدادات كان فيها UI جاهز مسبقاً لـ7 طرق
  حساب (MWL/ISNA/Egypt/Makkah/Kuwait/Qatar/Dubai) يُخزَن الاختيار في
  Hive (مفتاح calc_method) - لكن prayer_local_datasource.dart كان مثبَّتاً
  يدوياً على muslim_world_league دائماً، بلا أي قراءة لهذا الإعداد.
  يعني تغيير الإعداد من قِبل أي مستخدم لم يكن له أي أثر فعلي - خلل صامت.
- أُصلح جذرياً: أُنشئ calcMethodProvider (Riverpod v3 Notifier، لا
  StateNotifier القديم - المشروع يستخدم flutter_riverpod ^3.3.2) كمصدر
  حقيقة واحد بين الإعدادات والحساب الفعلي. شاشة الإعدادات تستدعي
  ref.read(calcMethodProvider.notifier).setMethod(id) عند التغيير،
  فينعكس فوراً في الصفحة الرئيسية بلا إعادة فتح التطبيق.
- تحقق من enum CalculationMethod الكامل في مكتبة adhan نفسها: وجدت 4
  طرق إضافية مدعومة لم تكن مستخدمة (karachi, singapore, turkey,
  moon_sighting_committee) - استُبعد عمداً tehran (فقه جعفري شيعي، خارج
  نطاق منهج سراج السني).
- التوسعة نُفذت: _calcIds صار 11 عنصراً، _resolveCalculationMethod يدعم
  الكل، وأُضيفت 4 مفاتيح ترجمة جديدة (calc_Karachi/Singapore/Turkey/
  MoonSighting) عبر جميع ملفات .arb الـ15 بترجمات حقيقية (لا نصوص
  مؤقتة) - عناية خاصة بالأردية/الفارسية/البنغالية للمناطق المعنية
  (كراتشي/سنغافورة). flutter gen-l10n نُفذ بنجاح، flutter analyze نظيف.
- الإحداثيات الافتراضية عند غياب GPS (24.7136, 46.6753 - الرياض) بقيت
  كما هي: سلوك احتياطي (fallback) واعٍ ومقصود، لا خطأ - موثَّق الآن رسمياً.

### الحالة الحالية الدقيقة (مهم لفهم الفجوة المتبقية)

- ✅ قسم الأحاديث مربوط بالرئيسية، delete_user يعمل، حساب الصلاة دقيق
  وقابل للتخصيص عالمياً (11 طريقة).
- ⚠️ القبلة لم تُفحص بعد هذه الجلسة (كانت ضمن نفس بند P0 الأصلي، لم
  نصل لها - أول شيء في الجلسة القادمة إن لم يُفتَح موضوع آخر أولوية).
- ⚠️ ملفات SIRAAJ_* المتبقية (Backend/Database/Flutter Architecture،
  Design_System، AI_Engine، Localization_Spec، Quality_Testing) لم
  تُفحَص بعد - بند P0 آخر مؤجَّل.

### استخراج أضواء البيان من شاملة - تقدم كبير، غير مكتمل

**المصدر النهائي المعتمَد: shamela.ws (كتاب رقم 22918، طبعة دار الفكر).**
- تفوّق بوضوح على مصدر Internet Archive (OCR خام): نص محرَر يدوياً
  مُشكَّل بالكامل، مقسَّم لفقرات مرقّمة (id="pN" على span.anchor داخل
  كل <p>، النص الفعلي في <p> الأب نفسه لا في span الفارغ - درس تقني
  مهم اكتُشف بالتجربة، وثّقناه في السكربت).
- فهرس الكتاب الكامل (سورة → رقم آية → رقم صفحة shamela) مُستخرَج
  ومُتحقَّق منه يدوياً (مثال: البقرة آية 282 = صفحة 181).

**السكربت النهائي (extract_adwaa_full_book.py) - يشتغل بالخلفية:**
- يمر على كل صفحات الكتاب (1 إلى 4343) بمنطق تصنيف مصحَّح: "حديث نبوي
  حقيقي" = وجود اقتباس «...» + فعل تخريج (أخرجه/رواه) + اسم كتاب سنة
  معاً في نفس الفقرة معاً (استبعدت كلمة "الحديث" المجردة بعد اكتشاف
  إيجابيات كاذبة كثيرة في التجربة الأولى على سورة الأنعام).
- الإسرائيليات (كعب الأحبار/وهب بن منبه/أهل الكتاب/التوراة) تُسجَّل
  بنوعها الصريح (type: israiliyyat) لا تُحذَف - قرار المستخدم الصريح:
  "حتى لو إسرائيليات... عادي تذكر ويوضح انها من مصادر إسرائيلية".
- دعم استئناف كامل (checkpoint.txt) - يمكن إيقافه بـCtrl+C ومتابعته
  من نفس الصفحة بالضبط. تأخير 1.5 ثانية احترامي بين كل طلب.
- **الحالة عند توقف الجلسة: صفحة 1943 من 4343 (44.7%)، 444 فقرة
  مرشّحة (حديث + إسرائيليات مجتمعين) في adwaa_citations_candidates.jsonl.**
- المخرجات: adwaa_full_book.jsonl (كل النص)، adwaa_citations_candidates.jsonl
  (المرشَحات فقط) - كلها في .gitignore، ليست جزءاً من مستودع الكود.

### المسار الجديد المُقرَّر للجلسة القادمة

1. **إن كان الاستخراج قد اكتمل (تحقق من checkpoint.txt أولاً):** مراجعة
   adwaa_citations_candidates.jsonl يدوياً - فصل الحديث الصحيح عن
   الإسرائيليات فعلياً بالقراءة (لا الاكتفاء بعلامات السكربت)، تجميع
   قائمة نظيفة (آية ↔ حديث ↔ نص ↔ تخريج) جاهزة للإدخال في verse_hadith_relations.
2. **إن لم يكتمل بعد:** إما تركه يكمل (يحتاج ~ساعة إضافية تقريباً من
   نقطة التوقف)، أو استئنافه فوراً بنفس الأمر (python3 extract_adwaa_full_book.py
   من نفس المجلد - يقرأ checkpoint.txt تلقائياً).
3. بناء جدول verse_hadith_relations في Supabase (مصمَّم سابقاً في وثيقة
   QKE، عمود source_ref TEXT NOT NULL لهذا الغرض بالضبط).
4. فحص القبلة (بند P0 مؤجَّل من هذه الجلسة).
5. مراجعة ملفات SIRAAJ_* المتبقية (بند P0 آخر مؤجَّل).
6. عند اكتمال ربط آية↔حديث لجزء معقول: تفعيل تبويب "أحاديث" الحقيقي في
   بوابة الآية (qke) - فقط للآيات الموثَّقة، البقية "لا تتوفر أحاديث
   مرتبطة" بصدق (نفس مبدأ أسباب النزول: 201 من 6236).

---

## تحسينات مؤجَّلة من "المرحلة الثانية من QKE" (يوليو 2026) - قرارات معمارية أكبر، لا تُنفَّذ الآن

أثناء بناء البحث النصي الكامل (tsvector + GIN + `arabic_normalize`)، ظهرت
فجوتان حقيقيتان تتجاوزان نطاق "تطبيع نص + فهرسة PostgreSQL" الذي طُلب
تحديداً - كلتاهما قرار معماري/تقني أكبر يستحق تقييماً مستقلاً لاحقاً،
لا اختراعاً مرتجَلاً الآن:

1. **مطابقة تقريبية (Fuzzy/Trigram) للكلمات الناقصة حرفاً كاملاً عن
   الرسم الرسمي.** مثال مُختبَر فعلياً: مستخدم يكتب "اله" (3 أحرف) بدل
   الرسم الصحيح "إله"/"الاه" (4 أحرف، الألف الوسطى ركن أصلي لا تشكيل
   زائد) - `arabic_normalize` الحالي لا يستطيع سد هذه الفجوة لأنها حرف
   كامل مفقود، لا تنويع تشكيل/ألف. الحل يحتاج `pg_trgm` (امتداد
   PostgreSQL آخر، فهرسة GIN/GiST مختلفة تماماً عن tsvector) أو قاموس
   تصحيح إملائي عربي مخصَّص - إضافة معمارية منفصلة، تستحق تقييم أداء
   وحجم فهرس مستقل قبل تبنّيها.
2. **مطابقة صرفية كاملة بالجذر (root-based stemming).** الجذع
   (stemmer) العربي المدمج في PostgreSQL (Snowball) يعالج بعض السوابق/
   اللواحق الشائعة لكنه ليس محرك تحليل صرفي حقيقياً (مثال: "الله" و"لله"
   الملتصقة بلام الجر يبقيان لفظين مختلفين تماماً في فهرسه). محرك صرفي
   حقيقي للنص القرآني تحديداً يحتاج الاستعانة بقاعدة جذور متخصصة (مثل
   بيانات Quranic Arabic Corpus الصرفية) - مشروع بحثي/هندسي منفصل بحجمه
   الخاص، ليس مجرد دالة تطبيع.

**الحالة:** البحث الحالي (tsvector + GIN + `arabic_normalize`) تحسّن
جذري موثَّق وحقيقي مقارنة بـILIKE البدائي السابق (كان بلا أي معالجة
تشكيل إطلاقاً) - يغطي الحالة الشائعة (كلمات صحيحة الرسم مع/بلا تشكيل،
بادئات جزئية أثناء الكتابة) بجودة عالية مُختبَرة فعلياً على البيانات
الحقيقية الكاملة. الفجوتان أعلاه تبقيان تحسينين مستقبليين موثَّقين،
لا حظر لإطلاق الميزة الحالية.
