# حالة جلسة "المرحلة الثانية من QKE" — يوليو 2026

توثيق حيّ لتقدّم هذه الجلسة تحديداً (3 بنود، كل بند فرع+PR منفصل، لا دمج
على main، لا لمس قاعدة إنتاج). يُحدَّث عند إنجاز كل بند، وملخص نهائي عند
اكتمال الجلسة بالكامل.

## البند 1: بحث نصي عربي كامل عبر PostgreSQL tsvector+GIN — ✅ مكتمل

- **الفرع/PR**: `qke/arabic-fulltext-search` — [PR #32](https://github.com/m-alajmi-eng/Siraj/pull/32)
- **الملفات**: `supabase/migrations/20260716101040_arabic_fulltext_search.sql`،
  `lib/features/search/presentation/providers/search_provider.dart`،
  تحديث `docs/03_MASTER_CHECKLIST.md` (صف "البحث") و`docs/ROADMAP.md`
  (قسم تحسينات مؤجَّلة).
- **ما تم**: استبدال `ILIKE '%...%'` البدائي بدوال بحث RPC مبنية على
  `tsvector` + فهارس GIN + دالة `arabic_normalize()` (تجريد تشكيل +
  توحيد ألف/همزة/ياء/تاء مربوطة + معالجة رسم عثماني خاص لكلمات كالصلاة/
  الزكاة/الحياة).
- **التحقق**: اختبار محلي كامل عبر `supabase start` (Docker) + تحميل
  نص القرآن العثماني الحقيقي الكامل (6236 آية) في قاعدة معزولة عن
  الإنتاج. اكتُشفت وأُصلحت خطأان حقيقيان أثناء الاختبار (تعيين حرف خاطئ
  في `translate()`، ونطاق regex خاطئ كان يحذف حروفاً عربية أساسية).
  تحقق ذاتي آلي على 295 آية عشوائية = 100% تطابق. اختُبر تطبيق ملف
  الـ migration النهائي من الصفر (`supabase db reset`).
- **قيود موثَّقة (لا حل مخترَع لها الآن)**: مطابقة تقريبية لحرف مفقود
  (fuzzy/pg_trgm)، وتحليل صرفي كامل بالجذر - كلاهما مسجَّل في
  `ROADMAP.md` كقرار معماري منفصل مستقبلي.
- **لم يُطبَّق على الإنتاج** — migration جاهز لمراجعة بشرية وتطبيق لاحق.

## البند 2: توحيد ayah_hadiths + verse_hadith_relations في kg_edges — ✅ مكتمل

- **الفرع/PR**: `qke/kg-edges-unify-relations`
- **الملف**: `supabase/migrations/20260716102020_kg_edges_unify_hadith_relations.sql`
- **ما تم**: جدول `kg_edges` موحَّد جديد (src_type/src_id/dst_type/dst_id/
  edge_type/source_reference/reviewed + أعمدة citation_* إضافية ضرورية
  لحفظ محتوى استشهادات أضواء البيان كاملاً بلا فقدان بيانات). **الجدولان
  القديمان لم يُحذَفا** — نسخة احتياطية كاملة حتى مراجعة بشرية.
  - `ayah_hadiths` → `edge_type='ayah_hadith'`، `dst_id`=hadith_id حقيقي
    (مفتاح خارجي فعلي)، `reviewed=false` (الجدول المصدر لم يملك عمود
    مراجعة أصلاً)، لكن سياسة RLS تُبقيه مرئياً دون قيد (كما كان بالضبط).
  - `verse_hadith_relations` → `edge_type` حسب citation_type
    (authentic_hadith_citation/israiliyyat_citation)، `dst_id=NULL`
    (لا كيان مطابَق)، والمحتوى الكامل محفوظ في أعمدة citation_*.
    **قيمة reviewed تُنسَخ كما هي من المصدر** (قرار بشري ماضٍ حقيقي، لا
    قرار جديد من الذكاء الاصطناعي) — موثَّق بالتفصيل في تعليقات الملف.
- **RLS**: سياسة واحدة تحاكي دمج القيدين القديمين معاً:
  `edge_type = 'ayah_hadith' OR reviewed = true`.
- **التحقق**: اختبار محلي كامل (`supabase start` + `supabase db reset`
  من الصفر) ببيانات مصطنعة تحاكي الجدولين المصدر: تأكّد نسخ البيانات
  بدقة، تأكّد عمل قيود CHECK (رفض قيم edge_type/dst_type غير صالحة)،
  وتأكّد فعلياً عبر `SET ROLE anon` أن سلوك العرض مطابق تماماً للجدولين
  القديمين مجتمعين (رابط حديث ظاهر دوماً، استشهاد مراجَع ظاهر، استشهاد
  غير مراجَع مخفي). تأكّد أيضاً أن PostgREST يحلّ الـembed
  `kg_edges → hadiths → hadith_books` عبر الـFK الجديد بشكل صحيح
  (لازم للبند 3 القادم).
- **لم يُطبَّق على الإنتاج** — migration جاهز لمراجعة بشرية وتطبيق لاحق.

## البند 3: واجهة "المحتوى ذو الصلة" تحت الآية — ✅ مكتمل

- **الفرع/PR**: `qke/related-content-ui`
- **الملفات**: `lib/features/qke/data/qke_repository.dart`،
  `lib/features/qke/presentation/screens/verse_portal_screen.dart`
- **ما تم**: بدل بناء واجهة جديدة من الصفر - "بوابة الآية" (QKE) كانت
  تملك أصلاً تبويب "أحاديث" جاهزاً (widget `_HadithsPage` يعمل فعلاً)،
  لكنه كان يقرأ مباشرة من `ayah_hadiths`/`verse_hadith_relations`.
  التعديل: إعادة توجيه مصدر البيانات لجدول `kg_edges` الموحَّد من البند 2
  فقط - بلا أي تغيير في تصميم الواجهة نفسها (نفس البطاقات، نفس شارة
  "استشهاد صحيح/إسرائيليات"، نفس حالة "لا تتوفر أحاديث" الصادقة عند عدم
  وجود بيانات).
  - استعلام واحد على `kg_edges` (`src_id`+`src_type='ayah'`) يجلب
    النوعين معاً (روابط الأحاديث + استشهادات أضواء البيان)، يُفرَّق
    بينهما في Dart حسب `edge_type` بدل استعلامين منفصلين على جدولين.
  - أُصلح تناقض بصري صغير كان موجوداً مسبقاً: تبويب "أحاديث" كان يُعرَض
    بأيقونة قفل (comingSoon) رغم أنه يعرض محتوى حقيقياً فعلاً - أُزيلت
    علامة comingSoon الآن لأنه فعلياً مكتمل ومربوط بمصدر بيانات حقيقي.
- **التحقق**: اختُبر عبر PostgREST مباشرة (curl على `supabase start`
  المحلي) أن الاستعلام المُدمَج يُرجع الحالتين الصحيحتين: صفّ برابط
  حديث حقيقي (`hadiths` embed كامل)، وصفّ باستشهاد (`hadiths: null`
  بأمان)، وأن RLS تُخفي الاستشهاد غير المراجَع فعلياً عن anon.
  `flutter analyze lib/features/qke/` نظيف.

---

# الملخص النهائي — جلسة "المرحلة الثانية من QKE" مكتملة بالكامل

**الحالة: البنود الثلاثة منجزة، كل واحد في فرع+PR منفصل، لا شيء دُمج
على main، لا شيء طُبِّق على قاعدة الإنتاج.**

| # | البند | الفرع | PR | CI |
|---|-------|-------|-----|-----|
| 1 | بحث نصي عربي كامل (tsvector+GIN) | `qke/arabic-fulltext-search` | [#32](https://github.com/m-alajmi-eng/Siraj/pull/32) | ✅ نجح (analyze+test+build، 15م50ث) |
| 2 | توحيد ayah_hadiths+verse_hadith_relations → kg_edges | `qke/kg-edges-unify-relations` | [#33](https://github.com/m-alajmi-eng/Siraj/pull/33) | ✅ نجح (15م44ث) |
| 3 | واجهة "المحتوى ذو الصلة" من kg_edges | `qke/related-content-ui` | [#34](https://github.com/m-alajmi-eng/Siraj/pull/34) | ✅ نجح (16م6ث) |

**تحديث نهائي**: CI (GitHub Actions: analyze + test + build APK) نجح
بالكامل على الفروع الثلاثة، بلا استثناء. البنود الثلاثة جاهزة فعلياً
لمراجعتك ودمجك بنفسك، بالترتيب (٣ يعتمد على ٢).

## قرارات معمارية مؤجَّلة (سُجِّلت في ROADMAP.md، لم تُخترَع حلولاً الآن)
- مطابقة تقريبية (fuzzy/pg_trgm) لكلمات ناقصة حرفاً كاملاً عن الرسم الرسمي.
- تحليل صرفي كامل بالجذر يتجاوز جذع Snowball العربي المدمج في PostgreSQL.

## منهجية التحقق المتّبعة عبر البنود الثلاثة
كل بند فيه تعديل Supabase اختُبر فعلياً محلياً قبل الكتابة النهائية عبر
`supabase start` (Docker) + بيانات حقيقية أو مصطنعة واقعية، وليس تخميناً
نظرياً. هذا كشف وأصلح أخطاء حقيقية (خطآن في دالة `arabic_normalize`
اكتُشفا فقط بتحميل نص القرآن الحقيقي الكامل). تعديلات Dart اعتُمد فيها
على `flutter analyze` محلياً مرة واحدة لكل تعديل (لا تكراراً)، ثم على CI
(GitHub Actions) للتحقق النهائي بدل بناء/اختبار محلي متكرر - يمكن
مراجعة حالة CI مباشرة عبر روابط الـPRs أعلاه.

## بنود خارج النطاق تماماً (كما طُلب - لم تُلمَس إطلاقاً)
أي محتوى جغرافي/تاريخي. لا حذف للجداول القديمة (ayah_hadiths،
verse_hadith_relations) - تبقيان كنسخة احتياطية حتى مراجعة بشرية.
