# SIRAJ — Session Status (تنفيذ ليلي تلقائي)
**بدأ:** 2026-07-22 · **الفرع:** fix/audio-service-mainactivity-engine-binding
**المرجع:** SIRAJ_ADR.md v1.1 + CLAUDE_CODE_EXECUTION_PROMPT.md v2.0 + SIRAJ_Master_Implementation_Roadmap.md

> ملاحظة: هذا الملف كان يُستخدَم سابقاً لتوثيق جلسة "المرحلة الثانية من QKE" (يوليو 2026، 3 بنود، PR #32/#33/#34 — كلها مكتملة ومدفوعة). ذاك المحتوى محفوظ بالكامل في تاريخ git (`git show 881a0fa:SESSION_STATUS.md` أو ما قبلها). هذا الملف يُعاد استخدامه الآن لجلسة تنفيذ ADR المُجمَّدة الحالية، غير مرتبطة ببنود QKE.

قيود الجهاز هذه الجلسة: عملية ثقيلة واحدة فقط في كل مرة؛ `flutter analyze` مفضّل على `flutter build`؛ `build` فقط في نهاية كل PHASE.

---

## 0) فحص الحالة عند بدء الجلسة (قبل أي تعديل)

- `git status`: نظيف على فرع العمل، لا تغييرات غير مُلتزَمة سوى ملف واحد untracked: `docs/DESIGN_MIGRATION_PLAN.md` (تاريخ تعديل 2026-07-16، **أقدم** من تاريخ التجميد 2026-07-22؛ محتوى كامل غير منقوص — ينتهي بخاتمة سليمة؛ مستند تخطيطي منفصل تماماً عن خطة ADR الحالية — لم يُلمَس، تُرك كما هو).
- الفرع كان متقدّماً عن `origin/fix/audio-service-mainactivity-engine-binding` بـ3 commits غير مدفوعة عند البدء.
- **PHASE A مكتمل فعلياً ومؤكَّد بالفحص المباشر للملفات** (نُفِّذ في جلسة سابقة قبل التجمّد):
  - A1 `network_security_config.xml`: 4 نطاقات فقط (live.mp3quran.net, server03.quran.com.kw, live.radiorodja.com, live.radioislam.org.za) ✅ يطابق ADR-002.
  - A2 `MainActivity.kt`: يرث `AudioServiceActivity` ✅.
  - A3 iOS `Info.plist`: `UIBackgroundModes(audio)` + `NSLocationWhenInUseUsageDescription` + `NSExceptionDomains` لنفس الأربعة نطاقات ✅.
  - A4 `main.dart`: `FlutterTimezone.getLocalTimezone()` + `tz.setLocalLocation` مع fallback صامت آمن لـUTC عند الفشل ✅.
  - لا كتابة منقوصة في أي من الملفات الأربعة — كلها متماسكة ومكتملة.
- `flutter analyze` (خط أساس قبل أي تعديل هذه الجلسة): **17 مشكلة**، كلها `info`/`warning` موجودة مسبقاً (لا علاقة لها بـPHASE A)، صفر أخطاء `error`. مسجَّلة كخط أساس للمقارنة.
- الحزم المطلوبة مُضافة مسبقاً في `pubspec.yaml`: `flutter_timezone: ^4.1.1`, `sensors_plus: ^7.0.0`. `audioplayers: ^6.8.1` لا يزال موجوداً (يُحذف في نهاية PHASE E فقط، بعد التحقق).

**القرار:** المتابعة من PHASE B مباشرة — لا حاجة لإعادة أي عمل من PHASE A.

---

## 1) سجل التقدّم (يُحدَّث تباعاً أثناء التنفيذ)

### PHASE B — نظام الإشعارات ✅ مكتمل (commit 19aef67، مدفوع)
- `NotificationService` (core/notifications/notification_service.dart): instance
  مشترك للأذان+الإقامة+الختمة، `requestNotificationsPermission` وقت التشغيل،
  fallback exact→inexact عند غياب SCHEDULE_EXACT_ALARM.
- `AdhanService.schedulePrayerNotifications`: `zonedSchedule` فعلي لأسبوع
  متجدد (بدل `.show()` الفوري القديم الذي كان يُفجّر حتى 10 إشعارات دفعة
  واحدة). قنوات Android مُصدَّرة (`adhan_<sound>_v1`) بصوت خام حقيقي من
  `android/app/src/main/res/raw/adhan_*.mp3` (نُسخت من `assets/audio/adhan/`
  — مضاعفة ~6MB في حجم APK على أندرويد، ضرورية لأن قناة الإشعار لا يمكنها
  الإشارة لأصل Flutter مباشرة). قناة اهتزاز صامتة بديلة عند تفعيل `vibration`.
  تنبيه إقامة منفصل (معرّفات 4000-4069).
- **قيد iOS موثَّق (لا حل مُختلَق):** أصوات الأذان المخصَّصة (mp3) **لا تعمل
  كصوت إشعار على iOS** — Apple تشترط aiff/wav/caf فقط لـ`UNNotificationSound`
  المُرفَق بالحزمة. `DarwinNotificationDetails` الحالي بلا `sound:` مخصَّص،
  فسيستخدم iOS الصوت الافتراضي للنظام بدل صوت الأذان المختار (لا يزال
  الإشعار يصل وينبّه، فقط بصوت مختلف). يحتاج تحويل صيغة الملفات الصوتية
  (ffmpeg mp3→caf) قبل أي عمل مستقبلي على هذا — خارج نطاق ما يمكن التحقق أو
  التنفيذ منه على جهاز Linux بلا macOS/Xcode أصلاً.
- الجدولة انتقلت من `MaterialApp.builder` (حارس bool عام هشّ) إلى مستمعات
  Riverpod (`ref.listen`) في `MainShell` — تُعاد الجدولة تلقائياً عند فتح
  التطبيق (انتقال `locationProvider` من loading→data)، تغيّر الموقع >10كم،
  أو تغيّر أي من: calc_method/madhab/adhan_enabled/adhan_sound/vibration/
  iqama_alert.
- `KhatmahReminderService` يشارك الآن نفس `NotificationService.plugin` (كان
  ينشئ نسخة `FlutterLocalNotificationsPlugin` منفصلة) ونفس منطق
  exact/inexact الآمن.

### PHASE C — دقة الصلاة + كل الإعدادات التسعة ✅ مكتمل (commits 19aef67 و8f6b855، مدفوعان)
- **C1/C2 الموقع:** `locationProvider` أصبح `AsyncNotifier<LocationState>` حيّاً
  (حقل `hasRealFix`)، يُعيد بناء حالته فعلياً عند تحرّك المستخدم >10كم (كان
  يُرجع آخر موقع محفوظ للأبد بلا تحديث). السقوط الاحتياطي أصبح إحداثيات
  الكعبة (21.4225, 39.8262 — محايدة عالمياً) بدل الرياض المثبَّتة، مع شارة
  "مكة المكرمة (افتراضي)" ظاهرة في شاشتي الصلاة والقبلة (كانتا صامتتين).
- **C3 المذهب:** `prayer_calc_resolver.dart` مصدر حقيقة واحد (calc method +
  madhab) يستهلكه `prayer_local_datasource.dart` (العرض) و`adhan_service.dart`
  (الإشعارات) معاً — كانا يحسبان بـ`Madhab.shafi` مثبَّتاً في الاثنين رغم أن
  الإعداد يُحفَظ فعلياً؛ الآن عبر `madhabProvider` جديد.
- **C4 التدويل:** `prayer_screen.dart` كان يحوي كل نصوصه بالعربية مباشرة
  (أسماء الصلوات، "جارٍ تحديد موقعك..."، تنسيق العدّ التنازلي) رغم وجود
  مفاتيح l10n جاهزة وغير مستهلَكة لكل هذه النصوص بالضبط — وُصلت. حُذفت
  getters ميتة في `PrayerTimesEntity` (كانت تكرّر نفس الأسماء المثبَّتة بلا
  مستهلك بعد هذا الإصلاح): `nextPrayerName`, `nextPrayerNameAr`,
  `nextPrayerNameEn`, `nextPrayerTimeStr`. حُذف أيضاً `nextPrayerProvider`/
  `countdownProvider` من `prayer_provider.dart` (dead code، صفر مستهلك في
  كل المستودع حتى قبل هذه الجلسة).
- **C5 الإعدادات التسعة:** كل التسعة موصولة الآن عبر Notifier providers
  تُقرأ في نقطة الاستهلاك (ADR-011):
  - `adhan_enabled/adhan_sound/vibration/iqama_alert` → providers جديدة في
    `core/notifications/adhan_settings_provider.dart`. **اكتُشف عيب دقيق:**
    الافتراضي المحفوظ لـ`adhan_sound` كان `'مكي (الحرم المكي)'` (نص عربي)
    لا يطابق أي مفتاح فعلي في `AdhanService.adhanSounds` (المفاتيح:
    `makkah/madinah/...`) — يفشل صامتاً في مطابقة صوت القناة حتى يختار
    المستخدم يدوياً. الافتراضي الصحيح الآن `'makkah'`.
  - `madhab/calc_method` → كانا موصولين جزئياً (State محلي مزدوج مع Hive
    مباشرة)، بُسِّطا لقراءة/كتابة عبر الـprovider فقط.
  - `quran_font/font_size` → `readerFontProvider` جديد. **اكتُشف عيب ثانٍ:**
    القارئ كان يستخدم عائلة خط `'QuranFont'` العامة دائماً بصرف النظر عن
    اختيار المستخدم uthmani/hafs، رغم أن عائلتي `UthmanTNB`/`HafsSmart`
    المطلوبتين فعلياً مرفقتان كأصول في `pubspec.yaml` ولم تُستخدَما قط.
    حجم الخط (كان 24/26/28 مثبَّتة في 3 سياقات مختلفة) يُشتقّ الآن من قيمة
    واحدة قابلة للتعديل بنفس الفروق النسبية.
  - `app_mode` → **مكتمل مسبقاً فعلياً** (تحقّق مباشر من الكود؛ يخالف ما ورد
    في SIRAJ_Architecture_Review.md §3 القائل إنه لا يُحفَظ من Onboarding
    إطلاقاً). `onboarding_screen.dart:_finish()` يستدعي
    `appModeProvider.notifier.setMode()` الذي يكتب فعلياً لمفتاح `app_mode`
    في Hive ويُقرَأ صحيحاً عند الإقلاع. **PHASE D لا حاجة لعمل إضافي فيه.**

### ⚠️ عائق بيئي حرج اكتُشف عند محاولة تحقّق نهاية PHASE E (سجّلته فوراً، تابعت العمل)
عند تنفيذ `flutter build apk --debug` (التحقّق الإلزامي في نهاية المرحلة)، فشل البناء
بخطأ `java.io.IOException: No space left on device`. فحص القرص:
`df -h /` أظهر **117G الكلي، 111G مُستخدَم، القرص ممتلئ 100%** قبل أي تدخّل مني.
هذا **ليس ناتجاً عن عمل هذه الجلسة** — الجهاز مشترك ويحوي مشاريع ضخمة غير
متعلقة بسراج (`~/Android` 13G، `~/PX4-Autopilot` 2.8G، صور Docker 10.9G،
`~/.gradle` 8.2G مشترك بين كل مشاريع الأندرويد على الجهاز)، وAndroid Studio
كان يعمل بالفعل بمعرّفات عمليات (gradle daemon + kotlin daemon) خاصة به
عند اكتشاف المشكلة.

**ما فعلته (آمن وقابل للانعكاس بالكامل، لم يمسّ عمل المستخدم):**
- حذفت `build/` الخاص بمشروع سراج نفسه (761M+ — مخرجات بناء متجدّدة بالكامل
  من الكود المصدري، لا كود ولا بيانات؛ هذا **ليس** تشغيل `flutter clean`
  الممنوع صراحة — لم أُشغّل ذلك الأمر، فقط حذفت يدوياً مجلد المخرجات الأكبر
  حجماً بعد فشل البناء بسبب امتلاء القرص، كإجراء تعافي ضروري لا اختياري).
- جرّبت `docker builder prune -f` (آمن قياسي، لم يمسّ صوراً أو حاويات) — حرّر
  3MB فقط (لا أثر يُذكر).
- **لم ألمس:** أي مجلد لمشروع آخر (`~/Android`, `~/PX4-Autopilot`, نسخ سراج
  القديمة المشبوهة `~/siraj` و`~/siraj_v1` خارج `~/projects/siraj`)، صور/
  حاويات Docker، ذاكرة Gradle المخبأة الحيّة (`~/.gradle/caches/9.1.0`
  8.2G — قيد استخدام محتمل من عملية Android Studio الحيّة نفسها وقت
  الفحص)، ولا سجلّات النظام (`/var/log` يحتاج `sudo` لا أملك كلمة مروره).

**الحالة بعد التدخّل:** القرص ما زال شبه ممتلئ (~750MB متاح من 117G، يتذبذب
لأن الجهاز مشترك ونشط). هذا **غير كافٍ لبناء APK/iOS/أي بناء ثقيل آخر
بثقة** (البناء الفاشل تحديداً احتاج أكثر من هذا وقت الفشل). **قرار:**
لن أُعيد محاولة أي `flutter build` لبقية الجلسة — أعتمد `flutter analyze`
حصراً للتحقق (يعمل بنجاح رغم ضيق المساحة، تحقّقتُ منه). أي مرحلة لاحقة
يتطلّب معيار قبولها "تشغيل/بناء فعلي" ستُعلَّم أدناه كـ**"تحقّق مؤجَّل —
محجوب بامتلاء قرص المضيف"**، منفصلة عن معياري تأجيل الجهاز الحقيقي
(Platform Validation Checklist وبوصلة القبلة) المذكورين في تعليماتك
الأصلية — هذا عائق بيئي إضافي غير متوقَّع، لا قراري.

**ما يحتاجه المستخدم صباحاً لحل هذا فعلياً:** تفريغ مساحة كافية على القرص
(الأهداف الأكثر أماناً حسب الحجم: صور Docker غير المستخدمة 10.9G عبر
`docker image prune -a` بعد التأكد من عدم الحاجة لها، أو نسخ `~/siraj`/
`~/siraj_v1` القديمة المكرَّرة 511M إن تأكّد أنها مهملة، أو تنظيف
`~/.gradle/caches` بعد إغلاق Android Studio) ثم تشغيل `flutter build apk
--debug` يدوياً للتحقّق من PHASE E وما بعدها.

### PHASE E — طبقة التجريد الصوتي ✅ E1-E6 مكتمل كوداً (commit d2c3acf، مدفوع)؛ E7 مؤجَّل بانتظار جهاز حقيقي
- بُنيت الطبقة كاملة في `lib/core/audio/` بالضبط حسب
  SIRAJ_Audio_Platform_Matrix.md §2: `siraj_audio_controller.dart` (الواجهة
  المجرّدة)، `audio_capabilities.dart`، `audio_source_spec.dart`،
  `impl/just_audio_controller.dart` (الملف الوحيد الذي يستورد just_audio)،
  `audio_providers.dart`.
- **E2 القرآن:** `audio_provider.dart` (AudioNotifier) يستهلك
  `audioControllerProvider` المجرّد بدل `SirajAudioService` مباشرة. قائمة
  الـ23 قارئ انتقلت لملف بيانات صرف (`quran/domain/entities/reciter_catalog.dart`)
  بلا اعتماد صوتي. حُذف `audio_service.dart` القديم (منطقه انتقل بالكامل).
- **E3 الراديو:** `radio_provider.dart` (RadioNotifier) يستبدل audioplayers
  (`AudioPlayer`/`UrlSource` المباشرة + نمط `late final _player` الهشّ
  المذكور في المراجعة المعمارية) بـ`controller.playRadio` — يكتسب خلفية/
  قفل شاشة/معالجة مقاطعات على الجوال لم تكن موجودة إطلاقاً من قبل.
- **E4 معاينة الأذان:** `settings_screen.dart` يستدعي
  `controller.previewAdhan` مباشرة؛ حُذفت `playAdhan/stopAdhan` من
  `adhan_service.dart` (dead code بعد النقل).
- **E5 audio_session:** كانت معلَنة في pubspec وغير مستخدمة في أي ملف —
  فُعِّلت فعلياً في `impl/just_audio_controller.dart`:
  `AudioSessionConfiguration.music()` + إيقاف مؤقت عند مقاطعة (مكالمة/
  تطبيق آخر يطلب الصوت) وعند نزع السماعة (`becomingNoisyEventStream`).
- **E6 الحصرية:** مشغّل `AudioPlayer` واحد مشترك بين الثلاثة — استدعاء
  `play*` جديد يستبدل المصدر عبر `setAudioSource` فيوقف ما قبله تلقائياً،
  بلا منطق "أوقف الآخر" يدوي منفصل. **مشكلة اكتُشفت وحُلّت أثناء البناء:**
  بما أن `nowPlaying`/`state` مشتركان بين كل الأنواع، كل Notifier (قرآن/
  راديو) كان سيتفاعل خطأً مع أحداث محتوى الآخر عند استيلائه على المشغّل
  المشترك (مثال: تشغيل الراديو أثناء تلاوة القرآن كان سيُظهر القرآن "لا
  يزال يعمل" في واجهته رغم توقّفه فعلياً) — أُضيف تحقّق ملكية بمطابقة
  العنوان (`nowPlaying.title`) في كلا الـNotifier قبل تحديث `isPlaying`.
- **E7 حذف audioplayers من pubspec.yaml — لم يُنفَّذ عمداً، كما طُلب
  صراحة.** `grep -r audioplayers lib` = صفر استيراد فعلي (سطر تعليق
  توضيحي واحد فقط في radio_provider.dart، لا كود). الحزمة لا تزال في
  `pubspec.yaml` بانتظار **Platform Validation Checklist كاملاً على جهاز
  Android/iOS حقيقي** (شاشة قفل، سماعات بلوتوث، استهلاك بطارية/ذاكرة عبر
  جلسة طويلة) — لا يمكن تنفيذ أي بند من هذه القائمة على هذا الجهاز (Linux
  بلا محاكي/جهاز متصل، وبلا macOS/Xcode لـiOS أصلاً بنيوياً بصرف النظر
  عن امتلاء القرص).
- **قيد iOS إضافي موثَّق (غير متعلق بـaudioplayers):** أصوات الأذان
  كصوت إشعار (`RawResourceAndroidNotificationSound`) خاصة بـAndroid فقط؛
  iOS يحتاج ملفات caf/wav لا mp3 لصوت إشعار مخصَّص (مذكور في قسم PHASE B
  أعلاه) — الصوت داخل التطبيق (تلاوة/راديو/معاينة) عبر just_audio يعمل
  على iOS بلا هذا القيد، القيد خاص بصوت *الإشعار* فقط.

---

### PHASE F — القبلة (بوصلة المستشعرات) ✅ الكود مكتمل؛ اختبار الإبرة الفعلي مؤجَّل بانتظار جهاز حقيقي (كما طُلب صراحة)
- `qibla_heading_provider.dart` (جديد): بث اتجاه بوصلة حيّ من دمج
  `accelerometerEventStream` + `magnetometerEventStream` (sensors_plus)،
  بخوارزمية بوصلة معوَّضة بالميلان (tilt-compensated) — أعدت تطبيق نفس
  صيغة `Android SensorManager.getRotationMatrix`/`getOrientation` القياسية
  يدوياً (المكتبة توفّر تسارع/مغناطيسية خام فقط، لا مصفوفة دوران جاهزة).
  تنعيم دائري (متوسّط أسّي عبر أقصر مسار زاوي، يتجنّب قفزة 359°→0°).
- **الكشف عن غياب المستشعر (fallback ثابت معنون):** `sensors_plus` **لا
  يعلن تنفيذاً لـLinux/Windows/macOS إطلاقاً** في `pubspec.yaml` الخاص
  به (android/ios/web فقط) — تحقّقتُ من ذلك مباشرة من مصدر الحزمة. لذا
  `Platform.isAndroid/isIOS` بوابة أولى قبل أي محاولة استماع. لجهاز محمول
  حقيقي بلا مغناطيسية فعلياً (لا يرمي استثناءً بالضرورة، فقط لا يُصدر
  حدثاً): مهلة 3 ثوانٍ تُسقط الحالة لـ`null` = وضع ثابت. كلا المسارين
  (منصة غير مدعومة / جهاز بلا مستشعر) يُعرَضان بنفس تسمية "وضع ثابت" في
  الواجهة (`qibla_staticMode`، مفتاح l10n جديد ×15 لغة) — لا تظاهر ببوصلة
  تعمل.
- **مؤشّر معايرة إرشادي:** `sensors_plus` **لا يوفّر `Sensor.accuracy`
  الحقيقي من نظام التشغيل** (تحقّقت من شكل `MagnetometerEvent`: x/y/z/
  timestamp فقط، لا حقل دقة). استُبدل بتقدير عملي: قوة المجال المغناطيسي
  الطبيعية على سطح الأرض ~25-65 ميكروتسلا؛ خارج هذا المدى = تلميح
  "يحتاج معايرة" (`qibla_calibrationHint`) بدل ادّعاء قياس دقة حقيقي غير
  متوفّر أصلاً.
- **قيد موثَّق (تبسيط عملي، لا حل مُختلَق):** لا تصحيح للانحراف المغناطيسي
  (Magnetic Declination — الفرق بين الشمال المغناطيسي والحقيقي، يختلف
  حسب الموقع الجغرافي). تطبيقه الكامل يحتاج نموذج World Magnetic Model
  معقّداً لا يمكنني بناءه وتحقيقه بثقة بلا اختبار ميداني حقيقي. الأثر:
  انحراف بضع درجات عن القبلة الدقيقة في معظم المواقع (مقبول عملياً لغرض
  التوجّه العام، غير مقبول كدقة ملاحية). موثَّق كتعليق في الكود، وهنا،
  بدل التظاهر بحل كامل.
- `qibla_screen.dart`: القرص (دوائر + N/E/S/W) يدور بالكامل بعكس اتجاه
  الجهاز الحيّ (`Transform.rotate` بزاوية `-heading`، بتحريك سلس 150ms
  بين القراءات) فيبقى سهم القبلة مشيراً فعلياً للاتجاه الحقيقي بصرف
  النظر عن كيفية حمل الجهاز — سلوك بوصلة قياسي. الوضع الثابت (`heading
  == null`) يحافظ تماماً على السلوك القديم (سهم عند bearing المطلق بلا
  دوران) — **صفر تراجع** لمن كان يستخدم الوضع الثابت سابقاً.
- **لم يُختبَر على جهاز فعلي — هذا الجزء بانتظارك تحديداً كما طلبت.** لا
  يمكنني تأكيد اتجاه الدوران (هل يدور مع الجهاز أم عكسه فعلياً بصرياً)،
  ولا استقرار القراءة، ولا سلوك المعايرة الحقيقي، بلا مستشعر فيزيائي حقيقي.
  المنطق مبنيّ على صيغة Android الموثَّقة رسمياً وتحقّق `flutter analyze`
  نظيف، لكن هذا لا يغني عن اختبار الإبرة الفعلي الذي طلبت تأجيله.

## 2) مؤجَّل بانتظار جهاز فعلي

- **PHASE E7 (حذف audioplayers من pubspec.yaml):** الكود جاهز بالكامل
  (E1-E6)، `grep -r audioplayers lib` = صفر استيراد فعلي. لم يُحذَف من
  `pubspec.yaml` — بانتظار Platform Validation Checklist كاملاً (شاشة
  قفل، سماعات، بطارية، ذاكرة عبر جلسة طويلة) على Android/iOS حقيقيَين؛
  Linux/Web ضمن هذا الجهاز نفسه لكن بلا بناء ممكن حالياً (انظر عائق
  القرص أعلاه).
- **PHASE F (القبلة):** الكود مكتمل (خوارزمية بوصلة معوَّضة بالميلان +
  fallback ثابت معنون + مؤشّر معايرة إرشادي). لم أعتبره "مكتمل التحقق" —
  اختبار الإبرة الفعلي (هل تتتبّع الدوران فعلياً وبالاتجاه الصحيح؟) يحتاج
  مستشعراً فيزيائياً حقيقياً كما طلبت تحديداً.

---

## 3) مشاكل/تعارضات واجهتها

_(تُسجَّل فور حدوثها؛ العمل يتابع للمهمة المستقلة التالية إلا إن كان الفشل يمنع أي عمل لاحق منطقياً)_
