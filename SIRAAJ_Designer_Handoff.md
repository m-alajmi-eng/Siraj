# حزمة تسليم للمصمم — تطبيق سراج (Siraj)

**تاريخ الإعداد**: 2026-09-08
**المصدر**: فحص مباشر للكود الفعلي بمجلد `lib/` والوثائق الموجودة بالمستودع — لا افتراض، كل رقم/حالة أدناه تحقَّقت منه شخصياً وقت الكتابة.

> **ملاحظة أمانة قبل البدء**: بعض التفاصيل أدناه تصحح افتراضات كانت متداولة سابقاً (مثال: نظام الثيمات الزمنية 6 حالات فعلية لا 8). التصحيح مقصود ومباشر لأن الهدف تصميم يبني على الواقع لا على انطباع سابق.

---

## القسم 1 — قائمة كاملة بكل الشاشات (22 قسماً فعلياً بـ`lib/features/`)

عدد الأقسام الفعلي **22** لا 21 (اكتشاف مباشر: بالإضافة للـ19 التي ذكرتَها، توجد أيضاً `language`، `more`، `onboarding` كأقسام كود مستقلة).

**رمز الحالة**: ✅ مكتمل وظيفياً · 🟡 جزئي (تفصيل الفجوة مذكور) · ⚪ معطَّل عمداً بانتظار قرار مستقبلي

| # | القسم (المسار بالكود) | الشاشات الفرعية | الحالة | ملاحظات للمصمم |
|---|---|---|---|---|
| 1 | **home** (`features/home`) | شاشة رئيسية واحدة | ✅ | تحتوي: تحية زمنية، تذكير الصلاة القادمة، بطاقة "متابعة القراءة"، شبكة وصول سريع لكل الأقسام الأخرى. **تعليق موثَّق بالكود نفسه** يحذّر من إعادة إدخال شريط بحث عائم سبق حذفه عمداً — راجعه قبل اقتراح شريط بحث بالرئيسية. |
| 2 | **prayer** (`features/prayer`) | شاشة واحدة | ✅ | أوقات الصلوات الخمس + تذكيرات/إشعارات مرتبطة بـ9 إعدادات (طريقة حساب، مذهب، صوت أذان، اهتزاز، تنبيه إقامة...). |
| 3 | **qibla** (`features/qibla`) | شاشة واحدة | ✅ | بوصلة تفاعلية (حساس مغناطيسي) + وضع ثابت احتياطي بلا حساس. |
| 4 | **athkar** (`features/athkar`) | 3 شاشات: قائمة أقسام رئيسية → أقسام فرعية → قائمة أذكار داخل قسم | ✅ | صوت تشغيل لكل ذكر (athkar_audio_provider). |
| 5 | **quran** (`features/quran`) | 3 شاشات: الرئيسية (قائمة سور) → قارئ السورة → بحث | ✅ | أكبر قسم كوداً (16 ملفاً، 2471 سطراً). القارئ يدعم تلاوة صوتية متصلة بلا فجوة بين الآيات (تلقائياً ينتقل للآية التالية). |
| 6 | **hadith** (`features/hadith`) | شاشتان: أقسام → قائمة أحاديث | ✅ | مدمج فعلياً كقسم أول داخل مسارات "المكتبة" بالراوتر (`/library/hadith/...`) رغم أن مجلد الكود منفصل تسمياً. |
| 7 | **khatmah** (`features/khatmah`) | 3 شاشات: قائمة خطط → إنشاء خطة → تفاصيل خطة | ✅ | نظام تذكير (reminder_service) لمتابعة ورد يومي. |
| 8 | **library** (`features/library`) | **6 شاشات**: الرئيسية → قسم → أنواع تصنيف → مؤلفون → عناصر مؤلف → عناصر تصنيف | ✅ | أكبر قسم عدد شاشات بالتطبيق. يجمع محتوى IslamHouse الخارجي + الحديث + المؤلفين. |
| 9 | **gateway** (`features/gateway`) | 5 شاشات: مقدمة → رحلة → مبادئ → موضوع → مكتبة | ✅ | "بوابة غير المسلمين" — خارج الشريط السفلي تماماً، شاشات كاملة الشاشة بزر رجوع (لا تتبع نمط تبويب). |
| 10 | **calendar** (`features/calendar`) | شاشة واحدة | ✅ | تقويم هجري. |
| 11 | **radio** (`features/radio`) | شاشة واحدة | ✅ | بث راديو حي، يتشارك نفس مشغّل الصوت مع القرآن (تشغيل أحدهما يوقف الآخر تلقائياً). |
| 12 | **mosques** (`features/mosques`) | شاشة واحدة | ✅ | قائمة مساجد قريبة (يحتاج صلاحية موقع). |
| 13 | **search** (`features/search`) | شاشة واحدة | ✅ | بحث موحَّد. |
| 14 | **stats** (`features/stats`) | شاشة واحدة | ✅ | إحصاءات استخدام (صلوات، أذكار...). |
| 15 | **sharing** (`features/sharing`) | شاشة واحدة | 🟡 | بطاقة مشاركة (قرآن/حديث/إلخ) — **قيد تقني معروف بالكود نفسه**: استخدام `BuildContext` بعد فجوة async بلا حارس `mounted` بموضعين (خطر تعطّل نادر لا يمنع التصميم، لكن يستحق إصلاحاً هندسياً لاحقاً). |
| 16 | **auth** (`features/auth`) | شاشتان: تسجيل دخول → حساب | ✅ | Google/Apple OAuth + دخول كضيف (مجهول) فعلي عبر Supabase. |
| 17 | **stories** (`features/stories`) | 4 شاشات: قصص عامة → قصص أطفال → قارئ سيرة → قارئ قصة | ✅ (مع 🟡 واحدة) | محتوى غني وحقيقي (1,588 ترجمة سيرة منشورة). **🟡 قارئ قصص الأطفال تحديداً**: تعليق TODO صريح بالكود ("قبل بناء APK: تفعيل/اختبار التمرير باللمس على جهاز حقيقي") — إيماءة السحب يمين/يسار موجودة كأزرار سهم مؤقتة، لم تُختبر لمسياً بعد. **لا تصمم افتراض أن السحب باللمس مضمون العمل بسلاسة قبل اختبار جهاز حقيقي (مرحلة لاحقة منفصلة).** |
| 18 | **qke** (`features/qke`) | شاشتان: بوابة الآية → قارئ أضواء البيان | 🟡 | تبويب "الحديث المرتبط" بداخل بوابة الآية **يعرض "قريباً — بعد استيراد الأحاديث" فعلياً** (شاشة `_ComingSoonPage` حرفية بالكود) — **لا تصمم محتوى فعلي لهذا التبويب، صمم حالة "قريباً" واضحة فقط.** |
| 19 | **settings** (`features/settings`) | شاشتان: الإعدادات → التراخيص | 🟡 | **مفتاحان معطَّلان عمداً بشارة "قريباً"**: "تباين عالٍ" و"تقليل الحركة" (إتاحة الوصول) — لا بنية تحتية فعلية لهما بالكود بعد. **لا تصمم أنهما يعملان — أبقهما بمظهر معطَّل بصرياً + شارة "قريباً" كما بالتصميم الحالي، أو صمم نسخة مستقبلية منفصلة إن رغب محمد بتفعيلهما لاحقاً.** |
| 20 | **language** (`features/language`) | شاشة واحدة | ✅ | اختيار اللغة الأول عند فتح التطبيق (قبل onboarding). |
| 21 | **onboarding** (`features/onboarding`) | شاشة واحدة، 4 صفحات داخلية (PageView): لغة → وضع → مذهب → موقع | ✅ | |
| 22 | **more** (`features/more`) | شاشة واحدة | ✅ | شبكة روابط لبقية الأقسام غير الظاهرة بالشريط السفلي. |

### بنية التنقّل الفعلية (مهم للمصمم)
الشريط السفلي يحوي **3 تبويبات فقط**: الرئيسية · القرآن · المزيد (قرار معتمَد موثَّق بالكود: `FREEZE-ADR-006 v2`). الأذكار والمكتبة **موجودان وقابلان للوصول الكامل** لكن عبر بطاقات بالرئيسية/المزيد لا كتبويب سفلي مستقل — لا تصمم 5 تبويبات سفلية.

---

## القسم 2 — القيود التقنية التي يجب معرفتها

### 1. معايير الأداء — ما هو موثَّق فعلياً، وما ليس كذلك
**رقم صريح واحد موثَّق بخطة الجودة الداخلية** (`docs/03_MASTER_CHECKLIST.md`): تقليب صفحات المصحف يجب أن يعمل بـ**60fps**. هذا الهدف الوحيد برقم محدد.

**لا يوجد رقم رسمي موثَّق لحد أدنى RAM أو حجم APK مستهدَف** بأي وثيقة بالمشروع (تحققت مباشرة، بحثت في كل الوثائق) — الافتراض الشائع "2GB RAM" **غير موجود كرقم رسمي**، فقط عبارة عامة متكررة "جهاز ضعيف/رخيص" كجمهور مستهدف بلا رقم دقيق. **التوصية للمصمم**: صمم بحذر افتراضي (تجنّب حركة/تأثيرات ثقيلة بلا داعٍ) دون التقيّد برقم محدد غير موجود فعلياً.

**أرقام حجم فعلية قِستُ بنفسي الآن**: APK إصدار غير موقَّع = 87.9MB، وإصدار موقَّع تاريخي سابق ≈167-170MB. لا حد أقصى مُعلَن رسمياً لهذا الرقم.

**⚠️ لم يُختبر التطبيق فعلياً على أي جهاز حقيقي حتى الآن بأي مقياس أداء** (زمن إقلاع، استهلاك ذاكرة، سلاسة حركة) — موثَّق صراحة بعدة تقارير داخلية. هذا **مؤجَّل عمداً لمرحلة لاحقة منفصلة** بقرار محمد — لا يمنع مرحلة التصميم، لكن يعني أن أي حركة/انتقال مقترَح لم يُتحقق من سلاسته على جهاز فعلي بعد.

### 2. نظام الثيمات الزمنية — **6 حالات فعلياً، لا 8**
هذا تصحيح مباشر لافتراض شائع. الكود الفعلي (`lib/core/theme/app_colors.dart`) يعرّف **6 لوحات ألوان فقط**، مرتبطة بأوقات الصلاة الفعلية (لا ساعة عامة ثابتة):

| الحالة | يبدأ عند | طابع اللون |
|---|---|---|
| الفجر (fajr) | أذان الفجر | أزرق ليلي داكن |
| الصباح (morning) | الشروق | بيج فاتح دافئ |
| الظهر (dhuhr) | أذان الظهر | بيج ذهبي |
| العصر (asr) | أذان العصر | برتقالي دافئ |
| المغرب (maghrib) | أذان المغرب | بنفسجي/وردي غروب |
| العشاء (isha) | أذان العشاء | أزرق داكن جداً/أسود |

كل شاشة تقريباً بالتطبيق (46 ملف كود) تستهلك هذا الثيم — **هذا هو نظام الثيمات الحقيقي الواجب احترامه دوماً، لا 8 حالات**.

**نظام منفصل تماماً موجود أيضاً وله استخدام محدود جداً**: تدرّج خلفية "سماء" بـ7 مراحل (`SirajSky` بملف `design_tokens.dart`، يضيف "الشروق" كحالة منفصلة) — **يُستخدم فقط بشاشة الرئيسية** (خلفية بصرية زخرفية للبطل العلوي)، ليس نظاماً عاماً. لا تخلط بين الاثنين.

**القرار المطلوب منك كمصمم**: نظام التوكنات الأساسي (الذهبي `#C9A96E`، خلفيات الكانفاس شبه السوداء، مقاييس المسافة/الخط/الظل) **موجود فعلياً بالكود ومكتمل** (`lib/core/theme/design_tokens.dart`، 206 سطر) — **ابنِ عليه، لا تعيد تصميمه من الصفر**. تحقق سابق (موثَّق بمستند داخلي منفصل) أكد أن نظام الألوان بأي تصميم مرجعي سابق كان مطابقاً حرفياً لهذه القيم بالفعل — أي أن طبقة الألوان/التوكنات مُقفَلة عملياً، والعمل التصميمي المطلوب هو على مستوى **التخطيط والمكوّنات والحركة لكل شاشة**، لا الألوان الأساسية.

### 3. نظام AppMode (خفيف/كامل)
موجود وفعّال بالكود (`lib/core/mode/`). **الوضع الكامل**: كل الأقسام الـ22 مفعَّلة تلقائياً بلا أي قرار من المستخدم. **الوضع الخفيف**: المستخدم يبني قائمته الخاصة يدوياً من بين **كل** الأقسام (بما فيها القرآن وأوقات الصلاة نفسها — لا نواة إجبارية)؛ الافتراضي عند أول تفعيل (قبل أي تخصيص): القرآن + الأذان + أوقات الصلاة فقط.

**أثر تصميمي مباشر**: أي قسم من الـ22 المذكورة أعلاه **قد لا يظهر إطلاقاً** لمستخدم بالوضع الخفيف لم يُفعِّله يدوياً. يجب أن تتضمن شاشة "الإعدادات"/"تخصيص الوضع الخفيف" تصميماً لقائمة تفعيل/تعطيل شاملة كل الـ22 قسماً (لا استثناء لأي قسم "أساسي").

### 4. دعم RTL/LTR — إلزامي وتلقائي فعلياً
**15 لغة مدعومة فعلياً وكاملة** (تحققت من كل ملف): العربية، الإنجليزية، الأردية، الفارسية، الإندونيسية، التركية، الفرنسية، البنغالية، الملايو، الهوسا، السواحيلية، الألمانية، الروسية، الصينية، الإسبانية. من بينها العربية/الأردية/الفارسية = RTL، والباقي LTR.

اتجاه الصفحة (RTL/LTR) **يُدار تلقائياً بالكامل عبر آلية Flutter القياسية** (`localizationsDelegates`+`supportedLocales`) — لا حاجة لتصميم "نسخة RTL" و"نسخة LTR" منفصلتين يدوياً لكل شاشة؛ الإطار يعكس التخطيط تلقائياً (الحواف، اتجاه الأيقونات ذات الاتجاه كالسهم، ترتيب العناصر). **لكن**: أي أيقونة توحي باتجاه (سهم للخلف/للأمام) يجب اختيارها من مكتبة تدعم الانعكاس التلقائي (Material Icons القياسية تفعل هذا)، لا صورة ثابتة الاتجاه.

### 5. حالات الواجهة الأربع — إلزامية لكل شاشة تعرض بيانات
النمط المعتمد فعلياً بالكود (24 ملفاً يستخدمونه حرفياً عبر `AsyncValue.when(data:, loading:, error:)` من مكتبة Riverpod): كل شاشة تجلب بيانات (من الشبكة أو التخزين المحلي) **يجب** أن تصمَّم لها الحالات الأربع التالية بوضوح، لا الحالة الناجحة فقط:
1. **تحميل (Loading)** — مؤشر انتظار واضح، لا شاشة فارغة صامتة.
2. **نجاح (Success)** — عرض البيانات الفعلي (الحالة الوحيدة المصمَّمة عادة بمعظم أدوات mockup الجاهزة).
3. **فارغ (Empty)** — لا بيانات حقيقياً (مثال: خطط ختمة صفرية، نتيجة بحث بلا نتائج) — رسالة/رسم توضيحي ودود، لا شاشة بيضاء تبدو معطوبة.
4. **خطأ (Error)** — فشل شبكة/خادم — رسالة واضحة + إن أمكن زر إعادة محاولة.

### 6. إمكانية الوصول (Accessibility)
- **لا رقم موحَّد صارم موثَّق لحجم اللمسة الأدنى بالمشروع نفسه**، لكن المرجع الوارد بمستند مقارنة التصميم الداخلي يذكر **48px** كهدف ("Larger Touch Targets")، وهذا يطابق فعلياً الحد الأدنى الافتراضي المدمج بإطار Material (`kMinInteractiveDimension = 48.0`) الذي يستخدمه التطبيق أصلاً. **اعتمد 48px كحد أدنى لأي عنصر تفاعلي (زر، أيقونة قابلة للنقر).**
- **⚠️ فجوة حقيقية وموثَّقة بالكود بوضوح**: معظم الأزرار "أيقونة فقط" بالتطبيق **لا تحمل تسمية accessible** (`Semantics`/`tooltip`) بعد — نمط جيد موجود بموضعين فقط كمرجع (`زر الرجوع المشترك بـAppScaffold`، وزرا البحث/تبديل الوضع بالرئيسية) لكنه **غير معمَّم على بقية التطبيق بعد**. **لا تفترض أن قارئ الشاشة (TalkBack/VoiceOver) سيعمل بسلاسة على كل تصميم مقترَح حتى تُضاف هذه التسميات هندسياً — هذا عمل برمجي منفصل، لكن صمّم كل أيقونة جديدة وكأنها ستحتاج تسمية نصية مرافقة بالمستقبل (لا تصمم أيقونة بلا أي سياق نصي مجاور يوضح وظيفتها).**
- التباين اللوني وتكبير الخط ("تباين عالٍ"/"تقليل الحركة") **معطَّلان بالكامل حالياً** (راجع بند settings بالقسم 1) — لا بنية فعلية بعد.
- **لم يُختبر أي من هذا فعلياً بقارئ شاشة حقيقي حتى الآن** (موثَّق صراحة، مؤجَّل لمرحلة اختبار الجهاز الحقيقي اللاحقة).

### 7. نظام التصميم (Design Tokens) الموجود فعلياً — ابنِ عليه
ملف واحد جامع (`lib/core/theme/design_tokens.dart`) يحوي كل ما يلي جاهزاً ومستخدَماً فعلياً بالتطبيق — **مرجعك الأساسي، لا تخترع مقاييس بديلة**:
- **الذهبي (العلامة التجارية)**: 10 درجات شفافية من نفس اللون الأساسي `#C9A96E`.
- **خلفيات الكانفاس**: 3 درجات شبه سوداء (`#000204`/`#010308`/`#020510`) — التطبيق ثيمه الأساسي داكن جداً بطبعه.
- **مقياس الأبيض الشفاف**: 18 درجة شفافية متدرجة لاستخدامات النصوص/الحدود فوق الخلفيات الداكنة.
- **ألوان دلالية**: نجاح/تحذير/خطأ/معلومة، كل منها بثلاث درجات (خلفية/حد/نص).
- **الخطوط**: أربعة عائلات مسمّاة بوضوح — `NotoSansArabic` (واجهة موحَّدة كل اللغات)، `NotoSans` (لاتيني)، `UthmanTNB` (القرآن حصراً)، `Amiri` (شعار العلامة فقط).
- **أحجام الخط**: 11 درجة (9 إلى 48 نقطة). **ارتفاعات السطر**: 6 درجات، منها درجة مخصصة للقرآن (1.65 vs. 2.3 للسور العادية) — فرق مقصود لسهولة القراءة القرآنية.
- **المسافات**: مقياس من 10 خطوات (4 إلى 64 نقطة). **الانحناءات (radius)**: نظامان متوازيان بالكود (قديم وأحدث) — استخدم الأحدث (`SirajRadiusFull`) للعمل الجديد.
- **الحركة**: 4 مدد زمنية معيارية (150/250/350/500 ملي ثانية) — لا تخترع مدداً عشوائية.
- **التخطيط**: حشو الصفحة 20، تباعد الأعمدة 9، فجوة الأقسام 32 (كل بالنقطة المنطقية).

---

## معلومة إضافية مهمة (خارج طلبك المباشر، لكن جوهرية للمصمم)
يوجد مستند داخلي منفصل (`docs/DESIGN_MIGRATION_PLAN.md`) يقارن مستودع تصميم مرجعي سابق (`DesignSIRAJHomeScreen`، ملفات `.tsx`) مقابل كل شاشة Flutter فعلية حالياً، شاشة بشاشة، مع تقدير جهد وتحديد فجوات دقيقة (نموذج بيانات ناقص، قرارات معمارية معلَّقة، فجوات إتاحة وصول محددة لكل شاشة تحديداً). إن كان هذا المصمم مستمراً على نفس ذلك المسار التصميمي المرجعي، فهذا المستند تحديداً هو أدق مرجع تفصيلي متاح لكل شاشة على حدة — يستحق الاطلاع عليه مباشرة قبل بدء أي شاشة بعينها.
